---
arxiv_id: '2011.11715'
title: Multi-task Language Modeling for Improving Speech Recognition of Rare Words
authors:
- Chao-Han Huck Yang
- Linda Liu
- Ankur Gandhe
- Yile Gu
- Anirudh Raju
- Denis Filimonov
- Ivan Bulyko
submitted: '2020-11-23'
categories:
- cs.CL
- cs.AI
- cs.LG
- cs.NE
- cs.SD
- eess.AS
arxiv_url: https://arxiv.org/abs/2011.11715
github_repo: ''
source: arxiv-html
converter: pandoc
llm_remediated: false
citations_resolved: 0/0
citations_resolved_at: '2026-07-07T19:50:50+00:00'
references_parsed: 0
arxiv_version: ''
---

Multi-task Language Modeling for Improving Speech Recognition\
of Rare Words
==============================================================

###### Abstract

End-to-end automatic speech recognition (ASR) systems are increasingly popular due to their relative architectural simplicity and competitive performance. However, even though the average accuracy of these systems may be high, the performance on *rare* content words often lags behind hybrid ASR systems. To address this problem, second-pass rescoring is often applied leveraging upon language modeling (LM). In this paper, we propose a second-pass system with multi-task learning, utilizing semantic targets (such as intent and slot prediction) to improve speech recognition performance. We show that our rescoring model trained with these additional tasks outperforms the baseline rescoring model, trained with only the LM task, by 1.4% on a general test and by 2.6% on a rare word test set in terms of word-error-rate relative (WERR). Our best ASR system with multi-task LM shows 4.6% WERR deduction compared with RNN Transducer only ASR baseline for rare words recognition.

Index Terms—  Language Modeling, Automatic Speech Recognition, Weighted Optimization and Multi-Task Learning.

## 1 Introduction

End-to-end neural systems have become widely used to solve a variety of natural language processing tasks. Particularly, in automatic speech recognition (ASR), systems like RNN-Transducer (RNN-T) \[1\] and Listen-Attend-Spell (LAS) \[2\], have received much attention. End-to-end system gained popularity in ASR in part because of their competitive performance and in part because of their architectural simplicity: instead of a number of different subsystems that go into a hybrid DNN-HMM system \[3\] (an HMM, a lexicon, a language model, etc.), end-to-end systems \[1, 2\] promise a single model that can be trained on paired sequences of acoustic features and words. However, despite the overall competitive performance, end-to-end systems have been shown to have a relatively low performance on *rare* words compared to hybrid systems, e.g., \[3\]. We believe that this may be due to the fact that hybrid systems take advantage of external knowledge outside of transcribed audio, such as human expert knowledge (lexicon) and larger language models trained on the unpaired text.

Second-pass rescoring is often used in ASR, which allows the use of a larger language model than would be practical within a single-pass system. In this work, we focus on improving a language model for second-pass rescoring. We target a specific application: an interactive voice assistant. In recent years this type of application has received much attention and widespread use, including systems such as Cortana, Siri, Alexa, Google Home, to name a few. Because these systems need to take action in response to user commands, they are often paired with a natural language understanding (NLU) module, which performs intent detection and slot tagging. In this work, we propose a multitask training of a language model that utilizes the intent and slots as additional targets at training time, and explore how this method impacts rare word accuracy of rescoring hypotheses generated by an RNN-T first-pass system.

The rest of this paper is organized as follows: in Section 2, we review related work, then we describe details of the proposed model in Section 3 and present experimental results in Section 4. Finally, we conclude in Section 5 and outline future work.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2011.11715/assets/x1.png)

Fig. 1: The proposed system includes (a) a first-pass RNN-Transducer and (b) a second-pass multi-task LM. The first and second-pass scores of the n-best ASR hypotheses are used for second-pass rescoring.

## 2 Related Work

Given the constrained search space in the second-pass, second-pass rescoring models are able to leverage larger and more complex models than in the first-pass. Previous works (e.g., \[4, 5, 6\]) have shown that including the second-pass rescoring model can result in significant word error rate (WER) improvements. Such second-pass rescoring systems are typically trained on various forms of textual data, including transcribed interactions with the ASR system, semi-supervised data, and other text corpora. More recently, several effective strategies have been identified for improving second-pass rescoring for end-to-end models, including RNN-T.  \[7, 5\] used a first-pass RNN-T model combined with a LAS component for second-pass rescoring and found that rescoring improved WER by up to 22% relative. Similarly, \[8\] used a deliberation model for second-pass rescoring and found an additional 5-11% relative WER reduction, compared to LAS rescoring.

The NLU module used in conjunction with the ASR system typically uses the outputs of the ASR system as its input to perform tasks like intent detection and slot tagging. Intent detection refers to the task in which the ASR output (e.g., *play the beatles*) is assigned an intent (e.g., *PlayMusicIntent*). Slot-filling refers to the task in which each word of the ASR output assigned a label (e.g., *play$`|`$other the$`|`$artist beatles$`|`$artist*). Numerous approaches, including support vector machines \[9\] and neural networks \[10\], have been used for intent detection, a classification task. Popular approaches for slot-filling, a sequence classification task, include conditional random fields \[11\] and recurrent neural networks (RNNs) \[12\].

Typically, the models used to perform intent detection and slot-filling are trained separately. However, recent work showed that it is possible to train a single model to perform both of these tasks \[13, 14\]. \[13\] demonstrated that the performance of a recursive neural network jointly trained on intent (and domain) detection and slot-filling was comparable to that obtained by models trained on the single-tasks alone. Similarly, \[14\] demonstrated that an attention-based RNN could be trained to perform both tasks, resulting in an error reduction of 23.8% on intents and 0.23% on slots. This and similar works \[13, 15\] do not seek to improve ASR metrics like WER; instead, the ASR system is held fixed, and its outputs are used as inputs to the multi-task NLU module, and there is no ASR-related loss to backpropagate.

Given that ASR errors can affect downstream intent detection and slot-filling tasks (which would use the erroneous ASR as input),  \[16, 17\] incorporated an ASR error task into the attention-based RNN, trained to perform both intent detection and slot-filling, from  \[14\]. They found that including the ASR error task not only resulted in improvements for the intent detection and slot-filling tasks but also significantly reduced the WER of the ASR engine by 4% absolute. Recently, \[18\] proposed an end-to-end spoken language understanding system in which the ASR and NLU modules are combined – the multi-task model showed improvements in both language understanding performance metrics, as well as ASR performance metrics. This suggests that information contained in the language understanding tasks can also benefit ASR.

Despite this, the focus of the majority of previous work on multi-task training with ASR and NLU tasks has typically been on the benefits to NLU. In our work, we are instead interested in how signals from NLU can improve ASR, specifically, with regards to language model performance. We demonstrate that a multi-task model trained with both NLU and ASR objectives results in better WER when used as a second-pass rescoring model than a model trained with an ASR objective alone.

## 3 Multi-task Language Modeling

### 3.1 Neural language model

We first define a standard neural language model (NLM) used in second-pass rescoring. Given a word sequence $`\mathbf{w}`$=($`w_{1},\ldots,w_{T}`$), the likelihood of the word sequence can be written as:

``` math
{{P\hspace{0pt}(\mathbf{w})} = {\prod\limits_{t = 1}^{T + 1}{P\hspace{0pt}\left( {w_{t} \mid w_{< t}} \right)}}}.
```

where the sequence $`\mathbf{w}`$ is padded with additional symbols $`w_{0}`$ and $`w_{T + 1}`$ to represent the start-of-sentence ($`<`$sos$`>`$) and end-of-sentence ($`<`$eos$`>`$) tokens. Following a similar architecture to  \[4\], the model contains a word embedding layer followed by $`K`$ long short-term memory (LSTM) \[19, 20\] layers. For the $`i`$-th word, $`P\hspace{0pt}\left( \mathbf{w}_{\mathbf{i}} \right)`$ is computed by taking a softmax over the unnormalized logits of all words, $`l_{i} = {e\hspace{0pt}x\hspace{0pt}p\hspace{0pt}\left( {{c_{K}^{T}\hspace{0pt}e_{w_{i}}} + b_{i}} \right)}`$, where $`c_{K}^{T}`$ represents the hidden context vector of the $`K^{t\hspace{0pt}h}`$ layer and $`e_{w_{i}}`$ is the output word embedding vector and $`b_{i}`$ is the bias value. The training loss, $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$, is cross-entropy loss over $`M`$ samples:

``` math
\mathbf{L}_{\mathbf{L}\mathbf{M}} = {- {\frac{1}{M}\hspace{0pt}{\sum\limits_{m}{\sum\limits_{t = 1}^{T + 1}{w_{t}^{m}\hspace{0pt}{\log p}\hspace{0pt}\left( {w_{t}^{m} \mid {w_{< t};\theta_{L\hspace{0pt}M}}} \right)}}}}}
```

### 3.2 Multi-task LM

Our goal is to integrate the intent detection and slot filling sub-tasks into the training of our NLM and use this model for rescoring the output of the first pass, as shown in Figure 1. We describe the shared architecture for each sub-task below:

Intent Detection (ID) is a task of predicting the intent of a given sequence of words $`\mathbf{w}`$. In the multi-task setting with an additional language model task, the hidden representations $`\mathbf{c}_{\mathbf{K}}`$ from the language model network (described in section 3.1) are fed into an intent detection specific network as shared features.

The cross-entropy training objective for $`\mathbf{L}_{\mathbf{I}\mathbf{D}}`$ over $`m`$ samples is defined in Eq. 3):

``` math
{\mathbf{L}_{\mathbf{I}\mathbf{D}} = {- {\frac{1}{m}\hspace{0pt}\left\lbrack {{log}\hspace{0pt}\left( {P\hspace{0pt}\left( {{\mathbf{i}\mathbf{n}\mathbf{t}\mathbf{e}\mathbf{n}\mathbf{t}} \mid {\mathbf{w};\theta_{I\hspace{0pt}D},\theta_{L\hspace{0pt}M}}} \right)} \right)} \right\rbrack}}},
```

where $`\theta_{L\hspace{0pt}M}`$ are the shared parameters between the LM task and ID task, and $`\theta_{I\hspace{0pt}D}`$ are model parameters specific to intent detection.

Slot Filling (SF) is a task for predicting the slot label sequence $`\mathbf{s}`$ corresponding to the word sequence $`\mathbf{w}`$. The slot label sequence is represented as $`\mathbf{s}`$=($`s_{1},\ldots,s_{T}`$), where $`s_{i}`$ is the slot label that aligns to input word $`w_{i}`$. Similar to intent detection, the hidden representations $`\mathbf{c}_{\mathbf{K}}`$ from language model network are input to a slot filling network. The training objective, $`\mathbf{L}_{\mathbf{S}\mathbf{F}}`$ over $`m`$ samples is defined in Eq. 4:

``` math
{\mathbf{L}_{\mathbf{S}\mathbf{F}} = {- {\frac{1}{m}\hspace{0pt}\left\lbrack {{log}\hspace{0pt}\left( {P\hspace{0pt}\left( {\mathbf{s} \mid {\mathbf{w};\theta_{S\hspace{0pt}F},\theta_{L\hspace{0pt}M}}} \right)} \right)} \right\rbrack}}},
```

where $`\theta_{S\hspace{0pt}F}`$ are the model parameters specific to slot filling network.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2011.11715/assets/x2.png)

Fig. 2: A illustration of multi-task LM. Two sub-networks, which include slot filling network (SFN) and intent detection network (IDN), use the shared features from RNN.

### 3.3 Training objective for multi-task learning

We introduce our training loss and optimization for our multi-task language model (*mtLM*) shown as Fig. 2 in this section. The network is trained by gradient descent to find the parameters that minimize the cross-entropy of the predicted and true distributions for the next word, intent class, and slot labels in a multi-task fashion. The total loss of mtLM, $`\mathbf{L}_{\mathbf{m}\mathbf{t}\mathbf{L}\mathbf{M}}`$, is defined by a normalized form:

``` math
{\mathbf{L}_{\mathbf{m}\mathbf{t}\mathbf{L}\mathbf{M}} = {{\alpha_{L\hspace{0pt}M}\hspace{0pt}\mathbf{L}_{\mathbf{L}\mathbf{M}}} + {\alpha_{I\hspace{0pt}D}\hspace{0pt}\mathbf{L}_{\mathbf{I}\mathbf{D}}} + {\alpha_{S\hspace{0pt}F}\hspace{0pt}\mathbf{L}_{\mathbf{S}\mathbf{F}}}}},
```

where $`\alpha_{L\hspace{0pt}M}`$,$`\alpha_{I\hspace{0pt}D}`$, and $`\alpha_{S\hspace{0pt}F}`$ are weight coefficients for each sub-tasks in the *mtLM* corresponding to Eq. (2) to Eq. (3). \

To optimize these weight coefficients, we use the linear searching method \[14\] as the baseline in which the intent and slot’s classification coefficients are set to zero from the beginning and linearly increased to one. Alternatively, we also apply the randomized weight majority algorithm (RWMA) from  \[21, 22\]. Compared to other gradient-based meta-learning methods, RWMA requires a lower model capacity and avoids executive computation while still smoothing the multi-task loss, as shown in  \[23, 24\].

  1. Initialize: $`{\overline{\alpha}}_{0} = (1,1,1)`$ and $`l_{0} = (0,0,0)`$

  2. For $`t = {1,2,\ldots,T}`$

  3.      Set $`\alpha_{t} = \left. {\overline{\alpha}}_{t}/Z_{t} \right.`$, where $`Z_{t} = \left. \sum{}_{i}{\overline{\alpha}}_{t}^{(i)} \right.`$.

  4.      Update loss $`\mathbf{L}_{\mathbf{m}\mathbf{t}\mathbf{L}\mathbf{M}}`$($`\alpha_{L\hspace{0pt}M}`$=$`\alpha_{t}^{1}`$, $`\alpha_{I\hspace{0pt}D}`$=$`\alpha_{t}^{2}`$,  $`\alpha_{S\hspace{0pt}F}`$=$`\alpha_{t}^{3}`$)

  5.      Compute the step-wise loss $`\mathbf{L}_{\mathbf{m}\mathbf{t}\mathbf{L}\mathbf{M}}^{\mathbf{s}\mathbf{t}\mathbf{e}\mathbf{p}}`$ and $`l_{t}`$.

  6.      If $`\rho`$($`\mathbf{L}_{\mathbf{L}\mathbf{M}}^{\mathbf{s}\mathbf{t}\mathbf{e}\mathbf{p}}`$,$`\mathbf{L}^{\mathbf{i}}`$) $`< 0`$ over $`\left. \sum{}_{i = t}\left( i - 9 \right. \right.`$, … , $`\left. i \right)\& t > 10`$:

  7.          Update rule $`\forall i`$, $`{\overline{\alpha}}_{t + 1}^{(i)} = {{\overline{\alpha}}_{t}^{(i)}\hspace{0pt}e\hspace{0pt}x\hspace{0pt}p\hspace{0pt}\left( {\left( {1 - \eta} \right)\hspace{0pt}l_{t}} \right)}`$.

  8.      Else:

  9.          Keep rule $`\forall i`$, $`{\overline{\alpha}}_{t + 1}^{(i)} =_{t}^{(i)}`$.

Algorithm 1 Randomized Weighted Majority Algorithm (RWMA) on Multi-Task LM Training

| Training Data | Training Loss | PPL_(norm) |
|----|----|----|
| D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ | $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ | 1.0 |
| D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ | $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ + $`\mathbf{L}_{\mathbf{I}\mathbf{D}}`$ | 0.995 |
| D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ | $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ + $`\mathbf{L}_{\mathbf{S}\mathbf{F}}`$ | 0.988 |
| D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ | $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ + $`\mathbf{L}_{\mathbf{I}\mathbf{D}}`$ + $`\mathbf{L}_{\mathbf{S}\mathbf{F}}`$ | 0.985 |
| D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ | $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ + $`\mathbf{L}_{\mathbf{I}\mathbf{D}}`$ + $`\mathbf{L}_{\mathbf{S}\mathbf{F}}`$ + RWMA | 0.982 |
| D$`{}_{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{n}\mathbf{s}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ | $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ | 0.985 |
| D$`{}_{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{n}\mathbf{s}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$$`\overset{}{\rightarrow}`$ D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ | $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ $`\overset{}{\rightarrow}`$$`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$+$`\mathbf{L}_{\mathbf{I}\mathbf{D}}`$+$`\mathbf{L}_{\mathbf{S}\mathbf{F}}`$+RMWA | 0.974 |

Table 1: Normalized perplexity (PPL_(norm)) evaluated on general domain data (D$`{}_{\mathbf{g}\mathbf{e}\mathbf{n}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$). PPL_(norm) is computed against the result from the first row. All models in the upper section are trained with the same multi-label NLU corpora (D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$) but different training objectives (LM, ID, SF) are used; lower section shows that for an LM pretrained with large transcription-only corpora D$`{}_{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{n}\mathbf{s}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$, finetuning using multi-task training with smaller corpus D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ still improves PPL_(norm).

## 4 Experiments

### 4.1 Datasets

We use $`\sim`$23k hours of far-field English utterances for voice control for training data to train our first-pass RNN-T model  \[25\]. For our second-pass NLMs, we used two training sets: (1) The more intent data (D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$) described in \[18\], with 11k hours of English sentences, each human-annotated for intent and slot; for our intent detection and slot-filling tasks, we used a subset of 25 intents and 20 slot labels from this dataset, based on the utility expected for our language modeling task. (2) A large-scale transcription-only corpora (D$`{}_{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{n}\mathbf{s}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$) with $`\sim`$ 90k anonymized English sentences. Transcription only data is several times more plentiful than data containing both intent and slot annotations; we use (2) to demonstrate that multi-task training, even compared to a language model leveraging several more times training data. We have two evaluation sets consisting of: (1) a multi-label NLU corpora (D$`{}_{\mathbf{g}\mathbf{e}\mathbf{n}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$) with $`\sim`$900k English sentences representing the general use case and (2) a rare words dataset (D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$) of $`\sim`$100 hours of utterances, where each utterance must contain a word in the long-tail distribution. All corpora contain de-identified utterances from voice assistant interactions.

### 4.2 Model architecture

The baseline NLM architecture \[4\] contains a word embedding matrix of dimension 512 and two LSTM layers of dimension 512 hidden, which is used as the shared feature representation $`c_{K}`$ for all three tasks. For the LM task, the shared representation is fed to a softmax layer predicting 10k wordpieces \[26\]. For the ID and SF tasks, we use two baseline architectures from  \[27\]: we use the aligned hidden representation input over time on $`c_{K}`$ (no attention) and weighted average of the hidden states (with attention), to encode the sequence representation to ID and SF outputs.

We further investigate an advanced architecture, projected attention layer, on NLU tasks with parallel-connected attention proposed by \[28\]. We adapt two projected attention layers followed by layer-norm over a weighted average layer of the hidden states $`c_{K}`$ of single attention head as the projected attention encoder used in ID and SF. All the encoders have been carefully fine-tuned with a similar model parameters.

### 4.3 Multi-task training objective

To find the optimal the weight coefficients between different loss functions for *mtLM* as in equation 5, we use linear searching method \[14\] as the baseline, and study the impact of randomized weight majority algorithm (RWMA) \[21, 22\]. The RWMA is described in Algorithm 1.

We first initialize a number of experts equal to the number of sub-tasks, $`d = 3`$. We choose $`T = 50`$, representing the total times for step-wise evaluation per epoch. We use a theoretical randomized coefficient $`\eta = \sqrt{\left. {2\hspace{0pt}{\log(d)}}/T \right.}`$ and expert weight $`\alpha \in R^{+}`$ with a range limit of (0.2, 0.6). $`l`$ is the probability of each expert showing degradations in performance over 1000 steps. Our online optimization is to minimize $`\mathbf{L}_{L\hspace{0pt}M}`$ loss, as shown in step 6 of Algorithm 1, where the individual coefficients $`{\overline{\alpha}}^{i}`$ would be adjusted with a negative Pearson correlation coefficient ($`\rho`$).

### 4.4 Second-pass Rescoring

As shown in Figure 1, the multi-task trained language model is used to rescore the n-best hypotheses of a first-pass ASR. In this paper, we rescore the output of an RNN-T model. The RNN-T encoder includes of five LSTM layers. Each layer has 1024 hidden units. The prediction network of RNN-T has two LSTM layers of 1024 units and an embedding layer of 512 units, following  \[25\].

The one-best sequence is obtained by combining the scores of RNN-T with the language model scores:

``` math
{y^{\ast} = {\arg{\max\limits_{y}\left( {\left. {{\log P}\hspace{0pt}\left( {y \mid x} \right)}/|w| \right. + {\lambda\hspace{0pt}{\log P_{L\hspace{0pt}M}}\hspace{0pt}(y)}} \right)}}}.
```

For LM scores, we use length-normalized probabilities from the language model network. We determine coefficient $`\lambda`$ using grid search over a development data set; it was fixed to $`0.006`$ for all reported experiments.

### 4.5 Results

|  |  |  |  |  |
|----|----|----|----|----|
| LM Model | D$`{}_{\mathbf{g}\mathbf{e}\mathbf{n}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$ |  | D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$ |  |
|  | PPL_(norm) | WERR (w/ RNN-T) | PPL_(norm) | WERR (w/ RNN-T) |
| No rescoring | - | baseline | - | baseline |
| D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$+ $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ | 1.0 | -0.75% | 1.0 | -1.0% |
| D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$+ $`\mathbf{L}_{\mathbf{m}\mathbf{t}\mathbf{L}\mathbf{M}}`$ | 0.982 | -2.5% | 0.984 | -2.2% |
| D$`{}_{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{n}\mathbf{s}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$+ $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ | 0.985 | -2.0% | 0.980 | -2.0% |
| D$`{}_{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{n}\mathbf{s}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ $`\overset{}{\rightarrow}`$D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$+$`\mathbf{L}_{\mathbf{m}\mathbf{t}\mathbf{L}\mathbf{M}}`$ | 0.974 | -3.4% | 0.971 | -4.6% |

Table 2: Rescoring results on general domain NLU data (D$`{}_{\mathbf{g}\mathbf{e}\mathbf{n}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$) and rare words test set (D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$). PPL values are normalized using the second row. WER relative¹ (WERR) is based on the first row as the baseline. The single-task models are trained with the language modeling loss $`\mathbf{L}_{\mathbf{S}\mathbf{F}}`$ and the multi-task models (rows 3 and 5) are trained with the multi-task loss $`\mathbf{L}_{\mathbf{S}\mathbf{F}}`$ with RWMA. The final row shows the effect of multi-task training when fine-tuing of a model trained with a large transcription corpora D$`{}_{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{n}\mathbf{s}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$.

#### 4.5.1 Model selection for multi-task LM

We explore two types of language models: the single-task LM (*stLM*) which only uses $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ for training, and the multi-task LM (*mtLM*) which is discussed in detail in 3.2 and can use two or more of $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$, $`\mathbf{L}_{\mathbf{I}\mathbf{D}}`$, and $`\mathbf{L}_{\mathbf{S}\mathbf{F}}`$ for training.

By evaluating perplexity (PPL) on general domain NLU data (D$`{}_{\mathbf{g}\mathbf{e}\mathbf{n}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$), we assess the benefit of multi-task training with additional ID and SF loss functions on the LM intrinsically. Throughout the paper, we report normalized PPL (PPL_(norm)), where PPL is normalized over a fixed baseline PPL for a given test set. Thus a PPL$`{}_{n\hspace{0pt}o\hspace{0pt}r\hspace{0pt}m}^{} <`$1 means that the candidate model’s PPL improves over that of the baseline model. As shown in the first four rows of Table 1, incorporating additional losses from ID and SF both improve PPL: we observe a PPL_(norm) reduction from 1.0 to 0.995 when we incorporate the ID loss ($`\mathbf{L}_{\mathbf{I}\mathbf{D}}`$) and a PPL_(norm) reduction from 1.0 to 0.988 when we incorporate the SF loss ($`\mathbf{L}_{\mathbf{S}\mathbf{F}}`$). We observe an additional benefit from training with all three losses for LM and NLU tasks, from 1.0 to 0.985. This suggests that NLU information can potentially benefit LM training. Additionally, the final row of the first section in Table 1 shows that optimizing $`\mathbf{L}_{\mathbf{m}\mathbf{t}\mathbf{L}\mathbf{M}}`$ using the RWMA shows additional benefit over the linear searching method described in Section 3.3.

The last two rows of Table 1 show that the benefit of multi-task training holds even when the language model is trained on a large amount of training data. Often only a subset of training data may have NLU annotations available. We leveraged additional (8x more) training data by using a transcription-only corpora D$`{}_{\mathbf{l}\mathbf{a}\mathbf{r}\mathbf{g}\mathbf{e}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ to train a *stLM*. The PPL resulting from this LM was comparable to the previous *mtLM*, trained with smaller data set. However, in the final row, we see that fine-tuning this *stLM* using multi-task training with a smaller corpus D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ improves PPL_(norm)) from 0.985 to 0.974. Thus, the benefit of multi-task training appears to hold even when we are able to leverage a large amount of training data. In the remaining experiments, we use the $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$, $`\mathbf{L}_{\mathbf{I}\mathbf{D}}`$, and $`\mathbf{L}_{\mathbf{S}\mathbf{F}}`$ to train *mtLM*.

| Encoder Architectures | PPL_(norm)$`\downarrow`$ | ER_(norm)$`\downarrow`$ | F1_(norm)$`\uparrow`$ |
|----|----|----|----|
| No Attention  \[27\] | 0.992 | 1.0 | 1.0 |
| Weighted-attention \[27\] | 0.988 | 0.975 | 1.013 |
| Projected-attention | 0.985 | 0.906 | 1.028 |

Table 3: Comparison of multi-task LMs trained with different encoders. Normalized PPL (PPL_(norm)), intent error rate (ER_(norm)), and F1 score of slot-filling (SF_(norm)) are evaluated on D$`{}_{\mathbf{g}\mathbf{e}\mathbf{n}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$. PPL_(norm) is computed against row 1 in Table 1. ER_(norm) and SF_(norm) are computed against the first row here.

Table 3 explores the result of using different types of feature encoders in multi-task training described in section 4.2. We show results using encoder architectures using aligned inputs with and without attention following  \[27\], as well as with projected-attention \[28\] with residual connections. We find that the projected-attention encoder (shown in Tab. 3) has better performance on normalized PPL, intent error rate, and slot prediction F1 score. Our final model thus uses the architecture with additional projected-attention layers.

#### 4.5.2 Impact on WER in second-pass rescoring

Next, we assess the benefit of multi-task training by using the *mtLM* as a second-pass rescoring model. In Table 2, we report both the normalized PPL and WER¹¹1The performance of the RNN-T speech recognition baseline system presented in this work is below 10% WER absolute. relative (WERR) on both the general test set (D$`{}_{\mathbf{g}\mathbf{e}\mathbf{n}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$) and rare words test set (D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$). The WERR is measured relative to the first-pass RNN-T baseline (no rescoring). As before, the PPL values are normalized using the result from the single task LM trained with D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ (second row). All multi-task LM results (rows 3 and 5) use the RWMA to optimize the multi-task loss, $`\mathbf{L}_{\mathbf{m}\mathbf{t}\mathbf{L}\mathbf{M}}`$.

First, we note that the baseline PPL for D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$ is around 3.3 times that of D$`{}_{\mathbf{g}\mathbf{e}\mathbf{n}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$, and the baseline WER is roughly 2.5 times higher for D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$ (not shown in table). This is expected as D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$ was selected to contain utterances with at least one slot content word in the long-tail distribution, while D$`{}_{\mathbf{g}\mathbf{e}\mathbf{n}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$ represents the general use case. Consequently, we also expect that the impact of multi-task training may be stronger for D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$, given the presence of high WER slot content. Next, comparing the single task LM D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$+ $`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$ and multi-task LM D$`{}_{\mathbf{N}\mathbf{L}\mathbf{U}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$+ $`\mathbf{L}_{\mathbf{m}\mathbf{t}\mathbf{L}\mathbf{M}}`$ trained on the smaller NLU dataset, we observe that multi-task training results in an improvement of 1.75% WERR on D$`{}_{\mathbf{g}\mathbf{e}\mathbf{n}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$ and 1.2% WERR on D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$ over the single-task model. We reported per-domain (intent) WER results in the rare word D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$ in Table 4, where the Automation, Video, and Music domains show major improvements compared with the baseline of ASR-LM without the proposed multi-tasking learning.

Critically, the same improvement trend is observed when using a single-task LM that is trained using all large transcription-only corpora D$`{}_{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{n}\mathbf{s}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$. In the final two rows of Table 2, we show that the model pre-trained on D$`{}_{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{n}\mathbf{s}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$ and fine-tuned using multi-task training with a smaller corpus performs better than the single-task model alone. The fact that multi-task LM can improve on pretrained single-task LM is important, as we typically have access to large amounts of unannotated data and a small amount of NLU labeled data. When having access to large transcription-only corpora D$`{}_{\mathbf{l}\mathbf{a}\mathbf{r}\mathbf{g}\mathbf{e}}^{\mathbf{t}\mathbf{r}\mathbf{a}\mathbf{i}\mathbf{n}}`$, one can see that the improvement due to multi-task training is larger for the rare words test set (-2.0% to -4.6% WERR) than the general test set (-2.0% to -3.4% WERR), underscoring the ability of multi-task LM to improve recognition on rare words. \

Domain Music Global Notification Automation Video WERR -3.7% -2.4% -3.4% -4.8% -3.8%

Table 4: Per-domain WER relative¹ (WERR) improvement of RNN-T with multi-task LM ( $`\mathbf{L}_{\mathbf{m}\mathbf{t}\mathbf{L}\mathbf{M}}`$) compared with RNN-T with LM ($`\mathbf{L}_{\mathbf{L}\mathbf{M}}`$) on top-5 domains (intents) of the rare words dataset D$`{}_{\mathbf{r}\mathbf{a}\mathbf{r}\mathbf{e}}^{\mathbf{t}\mathbf{e}\mathbf{s}\mathbf{t}}`$.

## 5 Conclusion

We demonstrate that incorporating the tasks of intent detection and slot-filling into language model training can reduce perplexity and WER for ASR systems when the language model is used as a second pass rescoring model. We find that the improvement in WER is more pronounced for rare words, likely due to improvements in recognition of slot content. The proposed rescoring framework also shows good potential to advance existing RNN Transfer systems for domain-depended or customized continuous speech recognition \[29\].

Moreover, we find the benefit of multi-task training holds even when comparing against a language model trained with large amounts of transcription data. Weighted optimization method \[21, 22\] is found to be a effective approach to determine the interpolation weights for the multi-task loss function for RNN Transducer modeling on rare word recognition.

Future work aims to utilize the slot and intent predictions that are output from our multi-task model. For example, this can be done by modifying the decoder to consider paths conditioned not only on only previous words but also on previous slot labels. Additionally, we can use the final intent prediction to bias the second-pass rescoring results. The privacy-preserving \[30, 31\] learning is also an important future direction considering domain-specific speech and acoustic processing applications. For example, when the language transcription comes from a different or low-resource domain \[32\] than the original speech corpora, it is necessary to provide reliable frameworks to measurement the information budgets of each training dataset. On the other hand, adaptation and knowledge distillation \[31\] are showing potential to get benefited from the multi-task learning techniques.

Finally, additional work can augment training with NLU data that is automatically labeled by intent or slot taggers to target further WER improvements. There are some recent works \[33, 34\] discovering the connections between RNN Transducer modeling and spoken languages understanding. We believe that the effeteness of multi-task learning discussed in this work could be useful to design large-scale pre-training methods for more down-stream applications.
## References
- \[1\] Alex Graves, “Sequence transduction with recurrent neural networks,” arXiv preprint arXiv:1211.3711, 2012.
- \[2\] W. Chan, N. Jaitly, Q. Le, and O. Vinyals, “Listen, attend and spell: A neural network for large vocabulary conversational speech recognition,” in 2016 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP), 2016, pp. 4960–4964.
- \[3\] Daniel Povey, Arnab Ghoshal, Gilles Boulianne, Nagendra Goel, Mirko Hannemann, Yanmin Qian, Petr Schwarz, and Georg Stemmer, “The kaldi speech recognition toolkit,” in In IEEE 2011 workshop, 2011.
- \[4\] Anirudh Raju, Denis Filimonov, Gautam Tiwari, Guitang Lan, and Ariya Rastrow, “Scalable multi corpora neural language models for asr,” arXiv preprint arXiv:1907.01677, 2019.
- \[5\] Tara N Sainath, Ruoming Pang, David Rybach, Yanzhang He, Rohit Prabhavalkar, Wei Li, Mirkó Visontai, Qiao Liang, Trevor Strohman, Yonghui Wu, et al., “Two-pass end-to-end speech recognition,” arXiv preprint arXiv:1908.10992, 2019.
- \[6\] Ankur Gandhe and Ariya Rastrow, “Audio-attention discriminative language model for asr rescoring,” in ICASSP 2020-2020 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP). IEEE, 2020, pp. 7944–7948.
- \[7\] Bo Li, Shuo-yiin Chang, Tara N Sainath, Ruoming Pang, Yanzhang He, Trevor Strohman, and Yonghui Wu, “Towards fast and accurate streaming end-to-end asr,” in ICASSP 2020-2020 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP). IEEE, 2020, pp. 6069–6073.
- \[8\] Ke Hu, Tara N Sainath, Ruoming Pang, and Rohit Prabhavalkar, “Deliberation model based two-pass end-to-end speech recognition,” in ICASSP 2020-2020 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP). IEEE, 2020, pp. 7799–7803.
- \[9\] Patrick Haffner, Gokhan Tur, and Jerry H Wright, “Optimizing svms for complex call classification,” in 2003 IEEE International Conference on Acoustics, Speech, and Signal Processing, 2003. Proceedings.(ICASSP’03). IEEE, 2003, vol. 1, pp. I–I.
- \[10\] Ruhi Sarikaya, Geoffrey E Hinton, and Bhuvana Ramabhadran, “Deep belief nets for natural language call-routing,” in 2011 IEEE International conference on acoustics, speech and signal processing (ICASSP). IEEE, 2011, pp. 5680–5683.
- \[11\] John Lafferty, Andrew McCallum, and Fernando CN Pereira, “Conditional random fields: Probabilistic models for segmenting and labeling sequence data,”  2001.
- \[12\] Kaisheng Yao, Geoffrey Zweig, Mei-Yuh Hwang, Yangyang Shi, and Dong Yu, “Recurrent neural networks for language understanding.,” in Interspeech, 2013, pp. 2524–2528.
- \[13\] Daniel Guo, Gokhan Tur, Wen-tau Yih, and Geoffrey Zweig, “Joint semantic utterance classification and slot filling with recursive neural networks,” in 2014 IEEE Spoken Language Technology Workshop (SLT). IEEE, 2014, pp. 554–559.
- \[14\] Bing Liu and Ian Lane, “Joint online spoken language understanding and language modeling with recurrent neural networks,” in Proceedings of the 17th Annual Meeting of the Special Interest Group on Discourse and Dialogue, Los Angeles, Sept. 2016, pp. 22–30, Association for Computational Linguistics.
- \[15\] Puyang Xu and Ruhi Sarikaya, “Convolutional neural network based triangular crf for joint intent detection and slot filling,” in 2013 ieee workshop on automatic speech recognition and understanding. IEEE, 2013, pp. 78–83.
- \[16\] Raphael Schumann and Pongtep Angkititrakul, “Incorporating asr errors with attention-based, jointly trained rnn for intent detection and slot filling,” in 2018 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP). IEEE, 2018, pp. 6059–6063.
- \[17\] Chao-Han Yang, Jun Qi, Pin-Yu Chen, Xiaoli Ma, and Chin-Hui Lee, “Characterizing speech adversarial examples using self-attention u-net enhancement,” in ICASSP 2020-2020 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP), 2020, pp. 3107–3111.
- \[18\] Milind Rao, Anirudh Raju, Pranav Dheram, Bach Bui, and Ariya Rastrow, “Speech to semantics: Improve asr and nlu jointly via all-neural interfaces,” InterSpeech, 2020.
- \[19\] Haşim Sak, Andrew Senior, and Françoise Beaufays, “Long short-term memory based recurrent neural network architectures for large vocabulary speech recognition,” arXiv preprint arXiv:1402.1128, 2014.
- \[20\] Sepp Hochreiter and Jürgen Schmidhuber, “Long short-term memory,” Neural computation, vol. 9, no. 8, pp. 1735–1780, 1997.
- \[21\] Nick Littlestone and Manfred K Warmuth, “The weighted majority algorithm,” Information and computation, vol. 108, no. 2, pp. 212–261, 1994.
- \[22\] Chao-Han Huck Yang, Jun Qi, Pin-Yu Chen, Yi Ouyang, I-Te Danny Hung, Chin-Hui Lee, and Xiaoli Ma, “Enhanced adversarial strategically-timed attacks against deep reinforcement learning,” in ICASSP 2020-2020 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP). IEEE, 2020, pp. 3407–3411.
- \[23\] Philip K Chan and Salvatore J Stolfo, “On the accuracy of meta-learning for scalable data mining,” Journal of Intelligent Information Systems, vol. 8, no. 1, pp. 5–28, 1997.
- \[24\] Mengye Ren, Wenyuan Zeng, Bin Yang, and Raquel Urtasun, “Learning to reweight examples for robust deep learning,” in International Conference on Machine Learning, 2018, pp. 4334–4343.
- \[25\] Jinxi Guo, Gautam Tiwari, Jasha Droppo, Maarten Van Segbroeck, Che-Wei Huang, Andreas Stolcke, and Roland Maas, “Efficient minimum word error rate training of rnn-transducer for end-to-end speech recognition,” arXiv preprint arXiv:2007.13802, 2020.
- \[26\] Mike Schuster and Kaisuke Nakajima, “Japanese and korean voice search,” in International Conference on Acoustics, Speech and Signal Processing, 2012, pp. 5149–5152.
- \[27\] Bing Liu and Ian Lane, “Attention-based recurrent neural network models for joint intent detection and slot filling,” InterSpeech, 2016.
- \[28\] Asa Cooper Stickland and Iain Murray, “Bert and pals: Projected attention layers for efficient adaptation in multi-task learning,” in International Conference on Machine Learning, 2019, pp. 5986–5995.
- \[29\] Linda Liu, Yile Gu, Aditya Gourav, Ankur Gandhe, Shashank Kalmane, Denis Filimonov, Ariya Rastrow, and Ivan Bulyko, “Domain-aware neural language models for speech recognition,” in ICASSP 2021-2021 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP). IEEE, 2021, pp. 7373–7377.
- \[30\] Chao-Han Huck Yang, Sabato Marco Siniscalchi, and Chin-Hui Lee, “PATE-AAE: Incorporating Adversarial Autoencoder into Private Aggregation of Teacher Ensembles for Spoken Command Classification,” in Proc. Interspeech 2021, 2021, pp. 881–885.
- \[31\] Scott Novotney, Yile Gu, and Ivan Bulyko, “Adjunct-emeritus distillation for semi-supervised language model adaptation,” Proc. Interspeech 2021, 866-870., 2021.
- \[32\] Chao-Han Huck Yang, Yun-Yun Tsai, and Pin-Yu Chen, “Voice2series: Reprogramming acoustic models for time series classification,” in Proceedings of the 38th International Conference on Machine Learning. 18–24 Jul 2021, vol. 139 of Proceedings of Machine Learning Research, pp. 11808–11819, PMLR.
- \[33\] Samuel Thomas, Hong-Kwang J Kuo, George Saon, Zoltán Tüske, Brian Kingsbury, Gakuto Kurata, Zvi Kons, and Ron Hoory, “Rnn transducer models for spoken language understanding,” in ICASSP 2021-2021 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP). IEEE, 2021, pp. 7493–7497.
- \[34\] Yinghui Huang, Hong-Kwang Kuo, Samuel Thomas, Zvi Kons, Kartik Audhkhasi, Brian Kingsbury, Ron Hoory, and Michael Picheny, “Leveraging unpaired text data for training end-to-end speech-to-intent systems,” in ICASSP 2020-2020 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP). IEEE, 2020, pp. 7984–7988.
