---
arxiv_id: '2307.03088'
title: Label-Synchronous Neural Transducer for End-to-End ASR
authors:
- Keqi Deng
- Philip C. Woodland
submitted: '2023-07-06'
categories:
- eess.AS
arxiv_url: https://arxiv.org/abs/2307.03088
github_repo: ''
source: arxiv-html
converter: pandoc
llm_remediated: false
citations_resolved: 0/0
citations_resolved_at: '2026-07-07T19:10:10+00:00'
references_parsed: 0
arxiv_version: ''
---

# Label-Synchronous Neural Transducer for End-to-End ASR

###### Abstract

Neural transducers provide a natural way of streaming ASR. However, they augment output sequences with blank tokens which leads to challenges for domain adaptation using text data. This paper proposes a label-synchronous neural transducer (LS-Transducer), which extracts a label-level encoder representation before combining it with the prediction network output. Hence blank tokens are no longer needed and the prediction network can be easily adapted using text data. An Auto-regressive Integrate-and-Fire (AIF) mechanism is proposed to generate the label-level encoder representation while retaining the streaming property. In addition, a streaming joint decoding method is designed to improve ASR accuracy. Experiments show that compared to standard neural transducers, the proposed LS-Transducer gave a 10% relative WER reduction (WERR) for intra-domain Librispeech-100h data, as well as 17% and 19% relative WERRs on cross-domain TED-LIUM2 and AESRC2020 data with an adapted prediction network.

Index Terms—  E2E ASR, neural transducer, domain adaptation

## 1 Introduction

End-to-end trainable (E2E) automatic speech recognition (ASR) simplifies traditional hidden Markov model (HMM)-based methods and directly transcribes speech into text \[1, 2\]. The neural transducer (NT) is a widely used E2E ASR structure with good streaming properties \[3\] compared to the attention-based encoder-decoder (AED) approach. While the AED can also be applied to streaming ASR \[4, 5\], it requires learning accurate monotonic alignments and always incurs significant latency \[6\]. When using a large amount of labelled training data, the E2E NT model has been reported to outperform HMM-based methods on some public data sets \[7\]. However, it still suffers from domain shifts \[8, 9\], and target-domain labelled data can not always be collected in quantity \[10\]. Therefore, it is more efficient to adapt transducer models to unseen domains using text-only data, which is often easier to obtain \[11\].

Domain adaptation is more challenging for E2E ASR than for the HMM-based approach \[12\], which uses a separate language model (LM) that can easily employ text-only data. Although the prediction network in NT models is analogous to the LM in terms of structure \[13\], it doesn’t perform solely as an LM \[13\] as it needs to coordinate with the acoustic encoder to generate both blank and non-blank tokens \[3\]. The prediction of blank tokens poses a challenge when adapting the standard NT using text-only data due to its inconsistency with the LM task \[3\]. However, the blank token is indispensable in the standard NT as it plays a key role in augmenting output sequences. This enables combining the frame-level encoder output with the label-level prediction network output \[2\].

The motivation of this paper is to modify the NT model while retaining the streaming properties, so that the blank token isn’t required. This makes the prediction network perform as an explicit LM, which is more adaptable with text-only data. This paper proposes a label-synchronous neural transducer (LS-Transducer), which extracts a label-level representation from the acoustic encoder output before combining it with the prediction network output, thus avoiding the need for blank tokens to align them. To generate this label-level encoder representation, an Auto-regressive Integrate-and-Fire (AIF) mechanism is proposed, which is extended from the Continuous Integrate-and-Fire (CIF) \[14\] approach but with improved efficiency and increased robustness to inaccurate unit boundaries. In addition, a streaming joint decoding method is designed to achieve better accuracy. ASR experiments with models trained on the LibriSpeech-100h data set \[15\] show that the proposed LS-Transducer gives reduced WER over standard NT models for both intra-domain and cross-domain scenarios.

The rest of this paper is organised as follows, Section 2 introduces general related work and Section 3 reviews the CIF on which the AIF technique is based. Section 4 describes the AIF and LS-Transducer methods. Section 5 details the experiments and Section 6 draws conclusions.

## 2 Related Work

Several studies have explored the use of text-only data for E2E ASR domain adaptation. One solution is LM fusion that incorporates an external LM into E2E ASR \[16, 17\], often using shallow fusion \[16\]. However, the E2E ASR model implicitly learns an internal LM characterising the source domain training data \[18\]. To address this issue, the internal LM of the E2E ASR can be estimated \[10, 18, 19, 20, 21, 22\]. For example, HAT \[20\] was proposed as an efficient way to estimate the internal LM by removing the effect of the encoder from the transducer network. However, internal LM estimation complicates the decoding process and accurate internal LM estimation is not always feasible due to domain mismatch \[11\]. Recently the factorised neural transducer \[3\] investigated fine-tuning the internal LM on target-domain text but it can give rise to intra-domain performance degradation. The use of Kullback-Leibler divergence regularisation can avoid this issue but limits how much the internal LM learns the target domain \[8, 9\]. Another approach is to use Text-to-Speech (TTS) to synthesise speech from target-domain text which is then used to fine-tune the transducer models \[23\], but this is computationally expensive and not flexible for fast adaptation \[3\].

## 3 Continuous Integrate-and-Fire (CIF)

Before introducing the LS-Transducer, details of CIF \[14\] are presented as background, since the AIF mechanism in the LS-Transducer is an extension of CIF. The aim of the CIF technique is to estimate a monotonic alignment for streaming ASR. As shown in Fig. 1 ‣ Label-Synchronous Neural Transducer for End-to-End ASR"), CIF first learns a weight $`\alpha_{t}`$ for each frame of the encoder output $`{\mathbf{e}}_{\mathbf{t}}`$. This weight $`\alpha_{t}`$ can be obtained by a sigmoid function, after mapping the encoder output $`{\mathbf{e}}_{\mathbf{t}}`$ to a one-dimensional scalar using convolutional or fully-connected layers \[14\] or even directly using a particular element of $`{\mathbf{e}}_{\mathbf{t}}`$ \[24\]. The weights are then accumulated across time and used to integrate the current label acoustic representation via a weighted sum. This continues until the accumulated weight is above a threshold of 1.0, at which point the current weight $`\alpha_{t}`$ is split into two parts: one part to make the accumulated weight for the current label to be exactly 1.0, with the remainder used for the integration of the next label. The CIF process then “fires” the integrated acoustic representation $`{\mathbf{c}}_{\mathbf{j}}`$ that corresponds to the label $`y_{j}`$ and resets the accumulation.

The CIF process is shown in Fig. 1 ‣ Label-Synchronous Neural Transducer for End-to-End ASR"), where the predicted weights $`\left( \alpha_{1},\cdots,\alpha_{T} \right)`$ could be, e.g., $`\left( 0.2,0.9,0.2,0.3,0.6,{0.1\hspace{0pt}\cdots} \right)`$. Then, $`\alpha_{2} = 0.9`$ is split into $`0.8`$ and $`0.1`$, so that the representation $`{\mathbf{c}}_{\mathbf{1}} = {{0.2\hspace{0pt}{\mathbf{e}}_{\mathbf{1}}} + {0.8\hspace{0pt}{\mathbf{e}}_{\mathbf{2}}}}`$ can be emitted. A similar situation arises for $`\alpha_{5} = 0.6`$, which is split into $`0.4`$ and $`0.2`$, so that $`{\mathbf{c}}_{\mathbf{2}} = {{0.1\hspace{0pt}{\mathbf{e}}_{\mathbf{2}}} + {0.2\hspace{0pt}{\mathbf{e}}_{\mathbf{3}}} + {0.3\hspace{0pt}{\mathbf{e}}_{\mathbf{4}}} + {0.4\hspace{0pt}{\mathbf{e}}_{\mathbf{5}}}}`$. Subsequent calculations of $`{\mathbf{c}}_{\mathbf{3}}`$, $`{\mathbf{c}}_{\mathbf{4}}`$, etc. proceed similarly until the end of the encoder output.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2307.03088/assets/cif8.png)

Fig. 1: Illustration of the CIF \[14\] mechanism. $`\oplus`$ and $`\otimes`$ denote addition and multiplication. $`\mathbf{E} = {({\mathbf{e}}_{\mathbf{1}},\cdots,{\mathbf{e}}_{\mathbf{T}})}`$ represents the encoder output and $`{\mathbf{α}} = {(\alpha_{1},\cdots,\alpha_{T})}`$ denotes predicted weights whose example values $`(0.2,0.9,0.2,0.3,0.6,{0.1\hspace{0pt}\cdots})`$ are also given.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2307.03088/assets/ls_transducer2.png)

Fig. 2: Illustration of the proposed LS-Transducer. FC is a fully connected layer. $`L`$ and $`V`$ are the length and vocabulary size of label-level logits. $`\oplus`$ denotes addition.

During training, to force the representations $`\mathbf{C} = \left( {\mathbf{c}}_{\mathbf{1}},\cdots,{\mathbf{c}}_{\mathbf{L}} \right)`$ to have the same length $`L`$ as the target sequence, a scaling strategy is employed: $`{\hat{\alpha}}_{t} = {\alpha_{t} \cdot \left( L/{\sum_{i = 1}^{T}\alpha_{i}} \right)}`$ where $`T`$ is the length of the encoder output and $`{\hat{\alpha}}_{t}`$ is used instead of $`\alpha_{t}`$ to extract $`\mathbf{C}`$. In addition, a quantity loss $`\mathcal{L}_{qua}`$ is used to supervise CIF to extract a number of integrated representations close to the target length $`L`$: $`\mathcal{L}_{qua} = \left| {{\sum_{i = 1}^{T}\alpha_{i}} - L} \right|`$, since the number of label representations generated is found by the accumulation $`\sum_{i = 1}^{T}\alpha_{i}`$ during decoding.

Note that CIF doesn’t always locate the real acoustic boundaries and accurately predict the text sequence length \[25\], especially when using units like BPE in English E2E ASR. Since the scaling strategy is used during training, a mismatch exists between training and decoding. Furthermore, CIF is a sequential method \[25, 26\] because it relies on knowing at which time step the previous label representation is emitted, followed by resetting the accumulation before extracting the next one, which can reduce training efficiency.

## 4 Label-synchronous Neural Transducer

This paper proposes a label-synchronous neural transducer (LS-Transducer), which is illustrated in Fig. 2 ‣ Label-Synchronous Neural Transducer for End-to-End ASR"). The LS-Transducer uses the proposed AIF mechanism to generate a label-level encoder representation before combining it with the prediction network output. To facilitate the adaptation of the prediction network with text-only data, the LS-Transducer combines, in an additive manner, the logits computed from the prediction network and the label-level encoder representations, rather than frame-level hidden features. Therefore, the joint network output is a 2-dimensional matrix $`{\mathbb{R}}^{L \cdot V}`$ as shown in Fig. 2 ‣ Label-Synchronous Neural Transducer for End-to-End ASR"), instead of a 3-dimensional tensor $`{\mathbb{R}}^{T \cdot L \cdot V}`$ with an extra time dimension in the standard neural transducer.

During training, the LS-Transducer uses the cross-entropy (CE) loss $`\mathcal{L}_{ce}`$ between the target text and prediction output by the joint network, as shown in Fig. 2 ‣ Label-Synchronous Neural Transducer for End-to-End ASR"). In addition, the CTC \[1\] loss $`\mathcal{L}_{ctc}`$ is also used by the encoder to help the model converge.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2307.03088/assets/aif11.png)

Fig. 3: Illustration of the proposed AIF. ·⃝ denotes dot-product attention that contains fully connected (FC) layers to map the keys and values to the same dimension as queries. The query $`{}{\mathbf{d}}_{\mathbf{j}}^{inter}`$ is the intermediate output of the prediction network.

### 4.1 Auto-regressive Integrate-and-Fire (AIF)

This paper proposes the AIF mechanism to generate label-level representations $`\mathbf{C} = \left( {\mathbf{c}}_{\mathbf{1}},\cdots,{\mathbf{c}}_{\mathbf{L}} \right)`$ from the acoustic encoder output $`\mathbf{E} = \left( {\mathbf{e}}_{\mathbf{1}},\cdots,{\mathbf{e}}_{\mathbf{T}} \right)`$ as in Fig. 3. AIF extends CIF and also uses accumulated weights $`\alpha_{t}`$ to locate boundaries and thus decide when to fire a label-level representation $`{\mathbf{c}}_{\mathbf{j}}`$. The difference is that when extracting the $`{\mathbf{c}}_{\mathbf{j}}`$, AIF uses dot-product attention instead of the weights $`\alpha_{t}`$ and takes the prediction network intermediate output as the query.

AIF generates the $`{\mathbf{c}}_{\mathbf{j}}`$ in an auto-regressive fashion, which has advantages over conventional CIF. First, AIF has higher training speed, because AIF can generate label-level representations in parallel with the teacher forcing technique by masking certain attention weights, while CIF is a sequential method, as mentioned in Sec. 3 ‣ Label-Synchronous Neural Transducer for End-to-End ASR"). Second, AIF does not need to employ the scaling strategy to enforce the extracted $`\mathbf{C}`$ to have the same length as the target, as the length of $`\mathbf{C}`$ is decided by the number of queries, so there is no mismatch between training and decoding. Third, although the boundaries found using the accumulated weights $`\alpha_{t}`$ are not always accurate, as shown in the dashed box of Fig. 3, AIF addresses this problem by taking the first frame as the left boundary when extracting the $`{\mathbf{c}}_{\mathbf{j}}`$.

To be more specific, inspired by \[24\], this paper employs a simple method to find the weight $`\alpha_{t}`$ by applying a sigmoid function to the last element $`e_{t,d}`$ of each encoder output frame $`{\mathbf{e}}_{\mathbf{t}}`$ ¹¹1AIF is not limited to this simple method of generating $`\alpha_{t}`$, other methods including convolutional or fully-connected layers could be used.:

``` math
\alpha_{t} = {{sigmoid}\hspace{0pt}\left( e_{t,d} \right)}
```

where $`d`$ is the dimension of $`{\mathbf{e}}_{\mathbf{t}}`$. Other elements of the encoder output are used to extract the label-level representation $`\mathbf{C}`$. The first step decides when to fire the label-level representation $`{\mathbf{c}}_{\mathbf{j}}`$ where $`j \in (1,L)`$, AIF achieves this by accumulating the weights $`\alpha_{t}`$ from left to right until it exceeds $`j`$, ²²2Note $`j`$ serves as both the index and threshold for the $`j`$-th representation. and this time step is recorded as $`T_{j}`$+1. If $`j`$ isn’t reached until all $`T`$ frames have been read, $`T_{j} = T`$. Second, the located $`\text{E}_{1:{T_{j},1}:{d - 1}}`$ is used as keys and values and the $`{\mathbf{c}}_{\mathbf{j}}`$ can be extracted via a dot-product attention operation as follows:

``` math
{\mathbf{c}}_{\mathbf{j}} = {{{{softmax}\hspace{0pt}\left( {{{\mathbf{d}}_{\mathbf{j}}^{inter} \cdot {FC}}\hspace{0pt}\left( \text{E}_{1:{T_{j},1}:{d - 1}} \right)^{\top}} \right)} \cdot {FC}}\hspace{0pt}\left( \text{E}_{1:{T_{j},1}:{d - 1}} \right)}
```

where query $`{}{\mathbf{d}}_{\mathbf{j}}^{inter}`$ is the prediction network intermediate output, and $`\text{E}_{1:{T_{j},1}:{d - 1}}\hspace{0pt}\text{=}\hspace{0pt}\left( {\mathbf{e}}_{{\mathbf{1},\mathbf{1}}:{{\mathbf{d}} - \mathbf{1}}},\cdots,{\mathbf{e}}_{{{\mathbf{T}},\mathbf{1}}:{{\mathbf{d}} - \mathbf{1}}} \right)`$ is mapped to the same dimension as $`{}{\mathbf{d}}_{\mathbf{j}}^{inter}`$ by fully connected layers, denoted as FC. This process is carried out incrementally until the last $`{\mathbf{c}}_{\mathbf{L}}`$ is generated.

In the example given in Fig. 3, the accumulated weight $`\alpha_{t}`$ exceeds $`1.0`$ at the $`5`$-th time step (i.e. $`{\sum_{i = 1}^{5}\alpha_{i}} > 1`$ and $`{\sum_{i = 1}^{4}\alpha_{i}} \leq 1`$), so the $`\mathbf{E}_{1:{4,1}:{d - 1}}`$ are used as the keys and values to extract $`{\mathbf{c}}_{\mathbf{1}}`$ with $`{}{\mathbf{d}}_{\mathbf{1}}^{inter}`$ as the query; similarly, accumulated weight $`\alpha_{t}`$ exceeds $`2`$ at the $`11`$-th time step, so the $`\mathbf{E}_{1:{10,1}:{d - 1}}`$ are the keys and values for $`{\mathbf{c}}_{\mathbf{2}}`$ with query $`{}{\mathbf{d}}_{\mathbf{2}}^{inter}`$. Subsequent extraction for $`{\mathbf{c}}_{\mathbf{3}}`$, $`{\mathbf{c}}_{\mathbf{4}}`$, etc. are similar.

When training the LS-Transducer, as for auto-regressive methods \[2, 27\], teacher forcing is used to input the ground truth into the prediction network. AIF also uses the quantity loss $`\mathcal{L}_{qua} = \left| {{\sum_{i = 1}^{T}\alpha_{i}} - L} \right|`$ to encourage the model to locate the correct boundaries. Hence, the overall training objective $`\mathcal{L}_{all}`$ of the LS-Transducer is:

``` math
\mathcal{L}_{all} = {{\gamma\hspace{0pt}\mathcal{L}_{ctc}} + {\left( {1 - \gamma} \right)\hspace{0pt}\mathcal{L}_{ce}} + {{\mu\hspace{0pt}\mathcal{L}_{qua}} \cdot L}}
```

where $`L`$ is target length, and $`\gamma`$ and $`\mu`$ are hyper-parameters.

### 4.2 Streaming joint decoding

With the AIF mechanism, LS-Transducer is naturally equipped for streaming decoding. Considering that the LS-Transducer uses the CTC branch to help model convergence, this paper further proposes a streaming joint decoding method, which computes a streaming CTC prefix score synchronously with LS-Transducer predictions to refine the search space and eliminate irrelevant alignments.

Standard CTC prefix scores depend on the whole encoder output E, hindering streaming decoding \[27\]. Suppose $`g`$ is a partial hypothesis, $`q`$ is a token appended to $`g`$, and the new hypothesis is denoted as $`h`$=$`g \cdot q`$. The CTC prefix scores $`S_{ctc}`$ are computed as:

$`{p_{ctc}\hspace{0pt}\left( h,{\left. \cdots \right|\text{E}} \right)} = {\sum\limits_{\nu \in {({U \cup {\lbrack{eos}\rbrack}})}}{p_{ctc}\hspace{0pt}\left( {\left. {h \cdot \nu} \right|\text{E}} \right)}}`$$`{S_{ctc}\hspace{0pt}\left( h,\text{E} \right)} = {\log\hspace{0pt}\left( {p_{ctc}\hspace{0pt}\left( h,{\left. \cdots \right|\text{E}} \right)} \right)}`$

where $`p_{ctc}`$ is the sequence probability ³³3 See \[27\] for detailed computation of CTC-based sequence probability. given by CTC, e.g. $`p_{ctc}\hspace{0pt}\left( {\left. {h \cdot \nu} \right|\text{E}} \right)`$ denotes the probability of $`h \cdot \nu`$ given whole encoder output E, and $`\nu`$ denotes all possible non-empty tokens ($`U`$ denotes normal tokens) and $`h \cdot \nu`$ means appending $`\nu`$ to $`h`$. Therefore, the CTC prefix score is computed as the accumulated probability of all sequences with $`h`$ as the prefix \[27\]. However, if $`q`$ (i.e. the last token of $`h`$) is end-of-sentence ($`\lbrack{eos}\rbrack`$), the CTC score is computed differently:

``` math
{S_{ctc}\hspace{0pt}\left( h,\text{E} \right)} = {\log\hspace{0pt}\left( {{\gamma_{T}^{(n)}\hspace{0pt}(g)} + {\gamma_{T}^{(b)}\hspace{0pt}(g)}} \right)}
```

where $`\gamma_{T}^{(n)}\hspace{0pt}(g)`$ and $`\gamma_{T}^{(b)}\hspace{0pt}(g)`$ are the forward probabilities \[1, 27\] of the $`g`$ over $`T`$ frames, with CTC paths ending with a non-blank or blank label, respectively. This process requires the complete encoder output E of $`T`$ frames which is inapplicable in streaming scenarios.

To achieve streaming joint decoding, inspired by \[5\], this paper uses a streaming score $`S_{ctc}\hspace{0pt}\left( h,\text{E}_{1:T_{h}} \right)`$ to approximate $`S_{ctc}\hspace{0pt}\left( h,\text{E} \right)`$, where $`T_{h}`$ is the maximum number of encoder output frames that can be accessed when predicting the new hypothesis $`h`$, which is decided by the accumulated weights $`\alpha_{t}`$ of AIF as shown in Fig. 3. However, when the corresponding CTC spike of token $`q`$ (i.e. the last token of $`h`$) does not appear during $`\text{E}_{1:T_{h}}`$, preliminary experiments showed this could greatly degrade the performance because the CTC score $`S_{ctc}\hspace{0pt}\left( h,\text{E}_{1:T_{h}} \right)`$ would be very likely to predict $`\lbrack{eos}\rbrack`$, in which $`h`$ is considered as complete given the limited input $`\text{E}_{1:T_{h}}`$. Previous work alleviated this problem by waiting until the corresponding CTC spike appeared before starting decoding \[4\] or switching to decoding the next block of speech when predicting the $`\lbrack{eos}\rbrack`$ label \[5\]. However, these methods are not feasible for the proposed LS-Transducer.

To address this problem, a streaming joint decoding method is proposed that modifies the computation of the CTC prefix scores for $`\lbrack{eos}\rbrack`$, which is shown as follows where $`h`$=$`g \cdot \lbrack{eos}\rbrack`$:

``` math
{S_{ctc}\hspace{0pt}\left( h,\text{E}_{1:T_{h}} \right)} = \begin{cases}
{{\log\hspace{0pt}\left( {p_{ctc}\hspace{0pt}\left( h,\left. \cdots \middle| \text{E}_{1:T_{h}} \right. \right)} \right)},} & {\ {T_{h} < T}\ } \\
{{\log\hspace{0pt}\left( {{\gamma_{T_{h}}^{(n)}\hspace{0pt}(g)} + {\gamma_{T_{h}}^{(b)}\hspace{0pt}(g)}} \right)},} & {\ {T_{h} = T}}
\end{cases}
```

This means that if the speech has not been fully read (i.e. $`T_{h} < T`$), $`h`$ won’t be considered complete and the score for $`\lbrack{eos}\rbrack`$ will be extremely small because CTC never sees the $`\lbrack{eos}\rbrack`$ label during training. This makes sense because the CTC prefix score should only consider ending prediction after loading all of the spoken utterance.

During streaming joint decoding, for the LS-Transducer, the predicted probability $`p_{{ls}\hspace{0pt}\text{-}\hspace{0pt}t}`$ is obtained by applying a softmax to the final logits output by the joint network, as shown in Fig. 3. It is calculated based on a limited input length and follows a chain rule. The streaming score $`S_{{ls}\hspace{0pt}\text{-}\hspace{0pt}t}`$ is then computed in the log domain as:

``` math
{S_{{ls}\hspace{0pt}\text{-}\hspace{0pt}t}\hspace{0pt}\left( h,\text{E}_{1:T_{h}} \right)} = {\sum\limits_{i = 1}^{n}{\log\hspace{0pt}\left( {p_{{ls}\hspace{0pt}\text{-}\hspace{0pt}t}\hspace{0pt}\left( h_{i} \middle| {h_{1},\cdots,h_{i - 1},\text{E}_{1:T_{i}}} \right)} \right)}}
```

where $`n`$ is the length of hypothesis $`h`$=$`g \cdot q`$ and $`T_{i}`$ is the corresponding right-hand boundary of the $`i`$-th label as determined by the proposed AIF. The overall streaming score $`S`$ is computed as:

``` math
{S\hspace{0pt}\left( h,\text{E}_{1:T_{h}} \right)} = {{\beta\hspace{0pt}S_{ctc}\hspace{0pt}\left( h,\text{E}_{1:T_{h}} \right)} + {\left( {1 - \beta} \right)\hspace{0pt}S_{{ls}\hspace{0pt}\text{-}\hspace{0pt}t}\hspace{0pt}\left( h,\text{E}_{1:T_{h}} \right)}}
```

where $`\beta`$ represents the weight of CTC prefix scores. Therefore, the streaming scores of the LS-Transducer $`S_{{ls}\hspace{0pt}\text{-}\hspace{0pt}t}\hspace{0pt}\left( h,\text{E}_{1:T_{h}} \right)`$ and the CTC branch $`S_{ctc}\hspace{0pt}\left( h,\text{E}_{1:T_{h}} \right)`$ are strictly synchronised.

## 5 Experiments

### 5.1 Corpus

ASR transducer models were trained on the “train-clean-100” subset of Librispeech \[15\], a read audiobook corpus, and its dev/test sets (i.e. “test/dev-clean/other”) were used for intra-domain evaluation. The training set transcripts and Librispeech LM training text were used as source-domain text data. In order to show the effectiveness of the LS-Transducer on domain adaptation, two out-of-domain test corpora were employed. The first was the TED-LIUM2 \[28\] dev/test sets, which is spontaneous lecture-style data. The training set transcripts and TED-LIUM2 LM training text were used as the target-domain adaptation text. The second was AESRC2020 \[29\] dev/test sets, which include human-computer interaction speech commands, and the target-domain text data was the training set transcriptions.

### 5.2 Model descriptions

All models were implemented based on the ESPnet \[30\] toolkit. Experiments used the raw speech data as input and 1000 modelling units as text output, including 997 BPE units and 3 non-verbal symbols: blank, unknown-character and sos/eos.

Three standard Transformer transducer (T-T) \[31\] models were built with streaming wav2vec 2.0 encoders and different prediction networks and compared to the proposed LS-Transducer. The T-T with an embedding layer as the prediction network is denoted as Stateless-Pred T-T (319M parameters); the T-T with a 6-layer 1024-dimensional LSTM prediction network is denoted as LSTM-Pred T-T (370M parameters); and the T-T with a 6-layer unidirectional Transformer prediction network (1024 attention dimension, 2048 feed-forward dimension, and 8 heads) is denoted as Transformer-Pred T-T (371M parameters). All three T-T baseline models used the wav2vec 2.0 encoder \[32\] (i.e. ”w2v_large_lv_fsh_swbd_cv”). A chunk-based mask \[7\] was implemented to achieve a streaming wav2vec 2.0 encoder during training, with a 320 ms average latency. The proposed LS-Transducer (373M parameters) had the same encoder as the three standard T-T baseline models and had a unidirectional Transformer prediction network that was the same as the Transformer-Pred T-T. The intermediate output of the $`3`$rd layer of the prediction network was used as the AIF mechanism query. The FCs in Fig. 2 ‣ Label-Synchronous Neural Transducer for End-to-End ASR") mapped dimensions from 1024 to 1000. In the Librispeech 100h data, the average number of frames corresponding to each unit is approximately 11, or 220 ms, i.e. less than 320 ms, so theoretically the AIF in the LS-Transducer did not introduce any additional latency. In Eq. 3 ‣ 4 Label-synchronous Neural Transducer ‣ Label-Synchronous Neural Transducer for End-to-End ASR"), $`\gamma`$ and $`\mu`$ were set to 0.5 and 0.05, respectively. The three standard T-T models also used the CTC branch with 0.3 weight to aid training. In Eq. 9, $`\beta`$ was set to 0.3 except for TED-LIUM2 which was set to 0.4. A Transformer-based offline AED model (394M parameters) was also built, which uses the same streaming wav2vec 2.0 encoder but was trained in an offline manner and decoded via offline CTC/attention joint decoding \[27\]. Building upon the Transformer-Pred T-T, both factorised T-T \[3\] (372M parameters) and HAT \[20\] (371M parameters) were implemented with the same encoder and prediction network (called vocabulary predictor in factorised T-T). The embedding layer of the vocabulary predictor is shared and used as the blank predictor in the factorised T-T.

A source-domain 6-layer Transformer LM was trained on the source-domain text data for 25 epochs and fine-tuned on the target-domain text for an extra 15 epochs as the target-domain LM. The source-domain LM was used to initialise the prediction network of the LS-Transducer but not for the three standard T-T models as this didn’t improve performance \[13\]. ASR models were trained for 40 epochs. When adapting the LS-Transducer prediction network, the first 3 layers were fixed, and the rest were fine-tuned on the adaptation text data with 50 epochs for AESRC2020 and 20 epochs for TED-LIUM2 data. Shallow fusion \[16\] was implemented with a 0.2 weight if using the target-domain LM for domain adaptation. The beam size was 10 during decoding.

|                                                     |       |       |       |       |
|-----------------------------------------------------|-------|-------|-------|-------|
|   Online ASR Models                                 | Test  |       | Dev   |       |
|                                                     | clean | other | clean | other |
| (Offline) W2v2 Transducer \[33\]      | 5.2   | 11.8  | 5.1   | 12.2  |
| (Offline) Conformer Transducer \[34\] | 5.9   | 16.9  | –     | –     |
| Chunked Conformer Transducer \[34\]   | 6.8   | 20.4  | –     | –     |
| (Offline) AED Model                                 | 4.4   | 11.3  | 4.2   | 11.3  |
| Stateless-Pred T-T                                  | 5.6   | 12.6  | 5.5   | 12.6  |
| LSTM-Pred T-T                                       | 5.3   | 12.5  | 5.1   | 12.5  |
| Transformer-Pred T-T                                | 5.1   | 12.0  | 4.9   | 12.0  |
| Proposed LS-Transducer                              | 4.6   | 11.4  | 4.4   | 11.2  |
|                                                     |       |       |       |       |

Table 1: Intra-domain WER on Librispeech dev/test sets for online transducer models trained on Librispeech-100h (train-clean-100 set).

### 5.3 Experimental results

Experiments compared the LS-Transducer with the standard T-T models for both intra-domain and cross-domain scenarios. Ablation studies were conducted to verify the effectiveness of the AIF and prediction network initialisation. Some related methods were also implemented and experimentally compared to the LS-Transducer.

#### 5.3.1 Intra-domain ASR

Table 1 lists intra-domain ASR results, in which our models achieved good results on the Librispeech-100h benchmark compared to various recent results. The Transformer-Pred T-T achieved the best results among the three standard T-T models, indicating that the prediction network with a strong Transformer structure was still effective in further improving ASR performance. In addition, the proposed LS-Transducer still clearly outperformed the strong standard Transformer-Pred T-T model with 10.2% relative WER reduction (WERR). Furthermore, the online LS-Transducer even performed virtually as well as the offline AED model, demonstrating the advantages of the LS-Transducer, including that the prediction network can be flexibly initialised with the source-domain LM. This initialisation technique has been shown highly effective in non-autoregressive E2E models for performance improvement \[24\], but is still challenging for auto-regressive E2E models such as Transformer-based AED \[26\] and standard neural transducer \[13\].

|  |  |  |  |  |
|----|----|----|----|----|
|   Online ASR Models | LS100$`\Rightarrow`$Ted2 |  | LS100$`\Rightarrow`$AESRC |  |
|  |   Test | Dev |   Dev | Test |
| Stateless-Pred T-T |   14.7 | 14.4 |   28.2 | 26.9 |
| +Target-domain LM SF |   12.9 | 12.9 |   24.9 | 23.6 |
| LSTM-Pred T-T |   14.7 | 14.4 |   28.8 | 27.5 |
| +Target-domain LM SF |   13.5 | 13.2 |   25.8 | 24.5 |
| Transformer-Pred T-T |   14.7 | 14.0 |   27.5 | 26.3 |
| +Target-domain LM SF |   13.6 | 12.9 |   24.7 | 23.6 |
| Proposed LS-Transducer |   14.4 | 13.6 |   26.9 | 25.6 |
| +Adapting Prediction Net |   12.2 | 11.7 |   23.0 | 21.3 |
| ++Target-domain LM SF |   11.5 | 11.0 |   22.1 | 20.4 |
|   |  |  |  |  |

Table 2: Cross-domain WER results on TED-LIUM 2 (Ted2) and AESRC2020 (AESRC) for online transducer models trained from Librispeech-100h (LS100). SF denoted shallow fusion\[16\].

#### 5.3.2 Cross-domain ASR

Experiments were conducted to compare cross-domain ASR performance on the TED-LIUM 2 and AESRC2020 corpora. As shown in Table 2, the proposed LS-Transducer gave the best performance on both cross-domain corpora, showing that LS-Transducer has promising general performance rather than overfitting to the source domain. After adapting the prediction network on the target-domain text data, further improvements could be obtained which surpassed the best result achieved by the three standard T-T models, with 17.0% and 19.0% relative WERR on TED-LIUM 2 and AESRC2020, respectively. Even when the standard T-T models used external target-domain LM to improve cross-domain performance through shallow fusion \[16\], there was still a performance gap of around 10% relative WERR compared to the proposed LS-Transducer with the prediction network adapted. In addition, the LS-Transducer could also use the external target-domain LM via shallow fusion to further improve the cross-domain performance.

Therefore, it can be concluded that the proposed LS-Transducer surpassed the standard T-T models in the source domain and is also very effective and flexible for domain adaptation. This is primarily due to the fact that LS-Transducer no longer predicts the blank label, thus making its prediction network work like standard LM.

#### 5.3.3 Ablation studies

|                               |       |       |       |       |
|-------------------------------|-------|-------|-------|-------|
|   Online ASR Models           | Test  |       | Dev   |       |
|                               | clean | other | clean | other |
| Transformer-Pred T-T          | 5.1   | 12.0  | 4.9   | 12.0  |
| Proposed LS-Transducer w/ AIF | 4.6   | 11.4  | 4.4   | 11.2  |
| Proposed LS-Transducer w/ CIF | 7.4   | 13.8  | 7.0   | 13.7  |
|                               |       |       |       |       |

Table 3: Ablation studies on the label-level encoder representation generation mechanism: intra-domain WER of the proposed LS-Transducer with the proposed AIF or normal CIF \[14\].

|                                 |       |       |       |       |
|---------------------------------|-------|-------|-------|-------|
|   Online ASR Models             | Test  |       | Dev   |       |
|                                 | clean | other | clean | other |
| Transformer-Pred T-T            | 5.1   | 12.0  | 4.9   | 12.0  |
| +pre-trained prediction network | 5.5   | 12.3  | 5.1   | 12.3  |
| Proposed LS-Transducer          | 4.6   | 11.4  | 4.4   | 11.2  |
|                                 |       |       |       |       |

Table 4: Ablation studies on prediction network initialisation: intra-domain WER of the standard Transformer T-T with or without prediction network pre-trained.

Ablation studies were conducted to evaluate the effectiveness of the proposed AIF mechanism. As shown in Table 3, the proposed AIF greatly outperformed CIF \[14\] and played an essential role that allows the LS-Transducer to outperform the strong Transformer-Pred T-T model. This is because the proposed AIF mechanism has many advantages that improve the WER over CIF, including that there is no mismatch between training and decoding and increased robustness to inaccurate acoustic boundaries.

In addition, since the prediction network of the LS-Transducer was initialised by a source-domain LM, further ablation studies were conducted to evaluate the effect of initialising the prediction network of the standard Transformer-Pred T-T model. As shown in Table 4, pre-training the prediction network of Transformer-Pred T-T did not improve performance but led to degradation, which is consistent with the conclusion in \[13\]. Therefore, the proposed LS-Transducer provides a natural way to use a pre-trained LM in E2E ASR.

#### 5.3.4 Comparison with related work

As further point of comparison, the factorised T-T \[3\] and HAT \[20\] models were implemented to compare to the LS-Transducer. Table 5 shows that HAT and factorised T-T slightly degraded intra-domain performance compared to strong Transformer-Pred T-T. Nevertheless, leveraging their advantages in domain adaptation (i.e., internal estimation or adaptation) compensates for this issue, leading to superior performance over Transformer-Pred T-T in cross-domain scenarios. However, the proposed LS-Transducer still clearly outperformed HAT and factorised T-T in both intra and cross-domain scenarios with WERRs between 8.1% and 15.4%.

The WER improvement brought by the proposed LS-Transducer over the HAT and factorised T-T is statistically significant at the 0.1% level using the matched-pair sentence-segment word error statistical test \[35\].

|                                   |            |       |      |       |
|-----------------------------------|------------|-------|------|-------|
|   Online                          | LS100 Test |       | Ted2 | AESRC |
| Neural Transducer Models          | clean      | other | Test | Test  |
| Transformer-Pred T-T Baseline     | 5.1        | 12.0  | 13.6 | 23.6  |
| HAT \[20\]          | 5.4        | 12.2  | 13.6 | 23.0  |
| Factorised T-T \[3\] | 5.4        | 12.4  | 13.3 | 22.5  |
| Proposed LS-Transducer            | 4.6        | 11.4  | 11.5 | 20.4  |
|                                   |            |       |      |       |

Table 5: WER results on intra (LS100) and cross-domain (Ted2 and AESRC) test data sets for different models trained from LS100. For cross-domain scenarios, the internal LM of HAT \[20\] was estimated, the vocabulary predictor of factorised T-T \[3\] was fine-tuned on target-domain text, and shallow fusion was used.

## 6 Conclusions

This paper proposes a label-synchronous neural transducer (LS-Transducer). Hence it does not require the prediction of blank tokens and is thus easy to adapt the prediction network on text data. An Auto-regressive Integrate-and-Fire (AIF) mechanism was designed that generates a label-level encoder representation which is combined with prediction network outputs while still allowing streaming. In addition, a streaming joint decoding method was proposed to refine the search space during beam search. Experiments show that the proposed LS-Transducer is very effective and flexible in terms of domain adaptation, and clearly outperformed the standard Transformer-Transducer (T-T) models in both intra-domain and cross-domain scenarios with up to 19.0% relative WER reduction. Furthermore, the LS-Transducer has a relative WER reduction between 8.1% and 15.4% compared with factorised T-T and HAT.
## References
- \[1\] A. Graves, S. Fernández, F. J. Gomez, and J. Schmidhuber, “Connectionist temporal classification: Labelling unsegmented sequence data with recurrent neural networks,” in Proc. ICML, 2006.
- \[2\] A. Graves, “Sequence transduction with recurrent neural networks,” ArXiv, vol. abs/1211.3711, 2012.
- \[3\] X. Chen, Z. Meng, S. Parthasarathy, and J. Li, “Factorized neural transducer for efficient language model adaptation,” in Proc. ICASSP, 2022.
- \[4\] H. Miao, G. Cheng, P. Zhang, T. Li, and Y. Yan, “Online hybrid CTC/attention architecture for end-to-end speech recognition,” in Proc. Interspeech, 2019.
- \[5\] E. Tsunoo, Y. Kashiwagi, and S. Watanabe, “Streaming transformer ASR with blockwise synchronous beam search,” in Proc. SLT, 2021.
- \[6\] C. Wang, Y. Wu, L. Lu, S. Liu, J. Li, G. Ye, and M. Zhou, “Low latency end-to-end streaming speech recognition with a scout network,” in Proc. Interspeech, 2020.
- \[7\] J. Li, Y. Wu, Y. Gaur, C. Wang, R. Zhao, and S. Liu, “On the comparison of popular end-to-end models for large scale speech recognition,” in Proc. Interspeech, 2020.
- \[8\] Z. Meng, Y. Gaur, N. Kanda, J. Li, X. Chen, Y. Wu, and Y. Gong, “Internal language model adaptation with text-only data for end-to-end speech recognition,” in Proc. Interspeech, 2022.
- \[9\] Z. Meng, T. Chen, R. Prabhavalkar, Y. Zhang, G. Wang, K. Audhkhasi, J. Emond, T. Strohman, B. Ramabhadran, W. R. Huang, E. Variani, Y. Huang, and P. J. Moreno, “Modular hybrid autoregressive transducer,” in Proc. SLT, 2023.
- \[10\] C. Choudhury, A. Gandhe, X. Ding, and I. Bulyko, “A likelihood ratio based domain adaptation method for E2E models,” in Proc. ICASSP, 2022.
- \[11\] E. Tsunoo, Y. Kashiwagi, C. P. Narisetty, and S. Watanabe, “Residual language model for end-to-end speech recognition,” in Proc. Interspeech, 2022.
- \[12\] J. Li, “Recent advances in end-to-end automatic speech recognition,” APSIPA Transactions on Signal and Information Processing, vol. 11, no. 1, 2022.
- \[13\] M. Ghodsi, X. Liu, J. Apfel, R. Cabrera, and E. Weinstein, “RNN-transducer with stateless prediction network,” in Proc. ICASSP, 2020.
- \[14\] L. Dong and B. Xu, “CIF: Continuous integrate-and-fire for end-to-end speech recognition,” in Proc. ICASSP, 2020.
- \[15\] V. Panayotov, G. Chen, D. Povey, and S. Khudanpur, “Librispeech: an ASR corpus based on public domain audio books,” in Proc. ICASSP, 2015.
- \[16\] J. Chorowski, D. Bahdanau, D. Serdyuk, K. Cho, and Y. Bengio, “Attention-based models for speech recognition,” Proc. NeurIPS, 2015.
- \[17\] A. Sriram, H. Jun, S. Satheesh, and A. Coates, “Cold Fusion: Training seq2seq models together with language models,” in Proc. Interspeech, 2018.
- \[18\] Z. Meng, N. Kanda, Y. Gaur, S. Parthasarathy, E. Sun, L. Lu, X. Chen, J. Li, and Y. Gong, “Internal language model training for domain-adaptive end-to-end speech recognition,” in Proc. ICASSP, 2021.
- \[19\] M. Zeineldeen, A. Glushko, W. Michel, A. Zeyer, R. Schlüter, and H. Ney, “Investigating methods to improve language model integration for attention-based encoder-decoder ASR models,” in Proc. Interspeech, 2021.
- \[20\] E. Variani, D. Rybach, C. Allauzen, and M. Riley, “Hybrid autoregressive transducer (HAT),” in Proc. ICASSP, 2020.
- \[21\] Z. Meng, S. Parthasarathy, E. Sun, Y. Gaur, N. Kanda, L. Lu, X. Chen, R. Zhao, J. Li, and Y. Gong, “Internal language model estimation for domain-adaptive end-to-end speech recognition,” in Proc. SLT, 2021.
- \[22\] W. Zhou, Z. Zheng, R. Schlüter, and H. Ney, “On language model integration for RNN transducer based speech recognition,” in Proc. ICASSP, 2022.
- \[23\] X. Zheng, Y. Liu, D. Gunceler, and D. Willett, “Using synthetic audio to improve the recognition of out-of-vocabulary words in end-to-end ASR systems,” Proc. ICASSP, 2021.
- \[24\] C. Yi, S. Zhou, and B. Xu, “Efficiently fusing pretrained acoustic and linguistic encoders for low-resource speech recognition,” IEEE Signal Process. Lett., vol. 28, pp. 788–792, 2021.
- \[25\] L. Yao, J. Song, R. Xu, Y. Yang, Z. Chen, and Y. Deng, “WaBERT: A low-resource end-to-end model for spoken language understanding and speech-to-BERT alignment,” ArXiv, vol. abs/2204.10461, 2022.
- \[26\] K. Deng, S. Cao, Y. Zhang, L. Ma, G. Cheng, J. Xu, and P. Zhang, “Improving CTC-based speech recognition via knowledge transferring from pre-trained language models,” Proc. ICASSP, 2022.
- \[27\] S. Watanabe, T. Hori, S. Kim, J. R. Hershey, and T. Hayashi, “Hybrid CTC/attention architecture for end-to-end speech recognition,” IEEE Journal of Selected Topics in Signal Processing, vol. 11, no. 8, pp. 1240–1253, 2017.
- \[28\] A. Rousseau, P. Deléglise, and Y. Estève, “Enhancing the TED-LIUM corpus with selected data for language modeling and more TED talks,” in Proc. LREC, 2014.
- \[29\] X. Shi, F. Yu, Y. Lu, Y. Liang, Q. Feng, D. Wang, Y. Qian, and L. Xie, “The accented English speech recognition challenge 2020: Open datasets, tracks, baselines, results and methods,” in Proc. ICASSP, 2021.
- \[30\] S. Watanabe, T. Hori, S. Karita, T. Hayashi, J. Nishitoba, Y. Unno, N. Enrique Yalta Soplin, J. Heymann, M. Wiesner, N. Chen, A. Renduchintala, and T. Ochiai, “ESPnet: End-to-end speech processing toolkit,” in Proc. Interspeech, 2018.
- \[31\] Q. Zhang, H. Lu, H. Sak, A. Tripathi, E. McDermott, S. Koo, and S. Kumar, “Transformer transducer: A streamable speech recognition model with transformer encoders and RNN-T loss,” in Proc. ICASSP, 2020.
- \[32\] W.-N. Hsu, A. Sriram, A. Baevski, T. Likhomanenko, Q. Xu, V. Pratap, J. Kahn, A. Lee, R. Collobert, G. Synnaeve, and M. Auli, “Robust wav2vec 2.0: Analyzing domain shift in self-supervised pre-training,” in Proc. Interspeech, 2021.
- \[33\] X. Yang, Q. Li, and P. C. Woodland, “Knowledge distillation for neural transducers from large self-supervised pre-trained models,” in Proc. ICASSP, 2022.
- \[34\] D. Albesano, J. Andrés-Ferrer, N. Ferri, and P. Zhan, “On the prediction network architecture in RNN-T for ASR,” in Proc. Interspeech, 2022.
- \[35\] D. Pallet, W. Fisher, and J. Fiscus, “Tools for the analysis of benchmark speech recognition tests,” in Proc. ICASSP, 1990.
