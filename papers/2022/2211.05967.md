---
arxiv_id: '2211.05967'
title: 'Align, Write, Re-order: Explainable End-to-End Speech Translation via Operation
  Sequence Generation'
authors:
- Motoi Omachi
- Brian Yan
- Siddharth Dalmia
- Yuya Fujita
- Shinji Watanabe
submitted: '2022-11-11'
categories:
- cs.CL
- eess.AS
arxiv_url: https://arxiv.org/abs/2211.05967
github_repo: ''
source: arxiv-html
converter: pandoc
llm_remediated: false
citations_resolved: 0/0
citations_resolved_at: '2026-07-07T19:20:11+00:00'
references_parsed: 0
arxiv_version: ''
---

Align, Write, Re-order: Explainable End-to-End Speech Translation\
via Operation Sequence Generation
==================================================================

###### Abstract

The black-box nature of end-to-end speech translation (E2E ST) systems makes it difficult to understand how source language inputs are being mapped to the target language. To solve this problem, we would like to simultaneously generate automatic speech recognition (ASR) and ST predictions such that each source language word is explicitly mapped to a target language word. A major challenge arises from the fact that translation is a non-monotonic sequence transduction task due to word ordering differences between languages – this clashes with the monotonic nature of ASR. Therefore, we propose to generate ST tokens out-of-order while remembering how to re-order them later. We achieve this by predicting a sequence of tuples consisting of a source word, the corresponding target words, and post-editing operations dictating the correct insertion points for the target word. We examine two variants of such operation sequences which enable generation of monotonic transcriptions and non-monotonic translations from the same speech input simultaneously. We apply our approach to offline and real-time streaming models, demonstrating that we can provide explainable translations without sacrificing quality or latency. In fact, the delayed re-ordering ability of our approach improves performance during streaming. As an added benefit, our method performs ASR and ST simultaneously, making it faster than using two separate systems to perform these tasks.

Index Terms—  speech recognition, speech translation

## 1 Introduction

Speech-to-text translation (ST) is an inherently compositional task consisting of first recognizing what was said and then translating the meaning. Cascaded approaches to ST follow this two-staged processing by first performing automatic speech recognition (ASR) and then passing those predictions to a machine translation (MT) model \[1, 2, 3, 4\]. On the other hand, end-to-end (E2E) approaches obviate the need for the intermediate ASR task \[5, 6, 7, 8\] but recent works have demonstrated improved performance using E2E multi-staged architectures which reflect the compositional nature of ST \[9, 10, 11, 12\]. However, E2E systems still offer reduced explainability compared to cascaded systems which can use neural MT approaches that explicitly emit word alignment information \[13\].

|  |
|----|
| ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2211.05967/assets/x1.png) (a) word alignment |
|   ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2211.05967/assets/x2.png) (b) absolute positional operation sequence |
| ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2211.05967/assets/x3.png) (c) relative shift operation sequence |

Fig. 1: Target sequences for our proposed E2E model ((b) and (c)) generated from source-target alignments (a). Symbols in brackets are post-editing operations dictating target word insertion order.

One direction towards E2E ST explainability is to simultaneously transcribe and translate using the same model. Prior works achieve this by interleaving chunks of ASR and ST predictions within one decoder \[14, 15\] or by allowing dedicated decoders to model latent alignments between the tasks \[16, 17, 18\]. However, it remains difficult to obtain word-level hard-alignments between ASR and ST sequences from such methods. Ideally, an explainable system would generate a source language word and its aligned target language word in tandem, but this would mean that the resultant translation sequence would be incorrectly ordered. Recent MT works offer a potential solution for this re-ordering problem via non-monotonic generation methods capable of insertion-based decoding \[19, 20, 21, 22, 23\]. However, these highly flexible approaches are not directly compatible with the monotonic design of speech processing systems, particularly for real-time streaming \[24, 25, 26, 27, 28, 29, 30\].

In this work, we seek to build explainable E2E ST models which 1) simultaneously generate ASR and ST predictions along with 2) word-level hard-alignment predictions while 3) avoiding degradation to offline and streaming translation quality. In particular, we are interested in modeling insertions to allow models to predict ASR tokens monotonically along with their corresponding ST tokens while remembering how to re-order the translation sequence later.

We propose to generate a sequence of tuples consisting of a source language word (for ASR), a corresponding target language word (for ST), and post-editing operations dictating the correct insertion point for the target word – we refer to these as operation sequences \[13\]. Our first operation sequence predicts the absolute positions of each target word within the eventual translation sequence; however the learned absolute positions may not generalize. To model insertion positions more relatively, we define a second operation sequence that models a shifting write header. Our relative shift operation sequence is inspired by \[13\] which was proposed for MT, and we extend this approach to ST by also performing simultaneous prediction of source language words. We demonstrate that our relative shift operation sequence enables explainable and high-quality E2E ST compared to baselines for offline and streaming settings with experiments on MuST-C \[31\]. Further, we find that the delayed re-ordering ability of our approach improves the partial hypothesis during streaming. Finally, an added benefit of our method is that simultaneous ASR/ST is faster than using two separate systems.

## 2 Background and Motivation

In this section, we review the black-box nature of direct approaches to E2E ST to motivate our explainable E2E ST approach in §3.

### 2.1 Direct E2E ST with ASR Multi-Tasking

The objective of the E2E ST is to directly predict a token sequence of the target $`(t)`$ translation $`\mathbf{y}^{(t)} = \left\{ {y_{l}^{(t)} \in \mathcal{Y}^{(t)}} \right\}_{l = 1}^{L^{(t)}}`$ from an input audio feature sequence $`\mathbf{X} = \left\{ {x_{i} \in \Re^{D^{(x)}}} \right\}_{i = 1}^{I}`$. $`I`$, $`L^{(s)}`$, and $`L^{(t)}`$ denote the sequence length of the input audio feature, transcription, and translation, respectively; $`D^{(x)}`$ denotes the dimension of input audio feature; and $`\mathcal{Y}^{(t)}`$ denotes the token vocabulary of target language. To predict translation, a neural network (NN) is trained to maximize the following objective:

$`\mathcal{L}^{({S\hspace{0pt}T})}`$$`= {{\log p}\hspace{0pt}\left( \mathbf{y}^{(t)} \middle| \mathbf{X} \right)}`$$`{= {\sum\limits_{m = 1}^{M}{{\log p}\hspace{0pt}\left( y_{m}^{(t)} \middle| {y_{1:{m - 1}}^{(t)},\mathbf{X}} \right)}}},`$

which is frequently modeled using the attention-based encoder-decoder (AED) architecture \[32, 33\]. AED models trained to maximize Eq. (2.1) try to directly predict a translation without knowledge of the source language, meaning it has to perform the complex mapping of a continuous speech input to a non-monotonic target language sequence. To alleviate this challenge, we can consider that each audio feature sequence $`\mathbf{X}`$ has a corresponding token sequence of the source $`(s)`$ transcription $`\mathbf{y}^{(s)} = \left\{ {y_{l}^{(s)} \in \mathcal{Y}^{(s)}} \right\}_{l = 1}^{L^{(s)}}`$. $`\mathcal{Y}^{(s)}`$ denotes the token vocabulary of source language. Therefore we apply a multi-tasked ASR and ST loss $`\mathcal{L}`$ as follows:

$`\mathcal{L}^{({A\hspace{0pt}S\hspace{0pt}R})}`$$`= {{\log p}\hspace{0pt}\left( \mathbf{y}^{(s)} \middle| \mathbf{X} \right)}`$$`{= {\sum\limits_{n = 1}^{N}{{\log p}\hspace{0pt}\left( y_{n}^{(s)} \middle| {y_{1:{n - 1}}^{(s)},\mathbf{X}} \right)}}}.`$$`\mathcal{L}`$$`{= {{\alpha\hspace{0pt}\mathcal{L}^{({S\hspace{0pt}T})}} + {\left( {1 - \alpha} \right)\hspace{0pt}\mathcal{L}^{({A\hspace{0pt}S\hspace{0pt}R})}}}},`$

where $`\alpha`$ denotes a controllable interpolation (we use 0.3 in this study). Using the NN trained with Eq. (3), we can obtain hypothesized translation token sequence $`{\hat{\mathbf{y}}}^{(t)}`$ using the following decision:

$`{{\hat{\mathbf{y}}}^{(t)} = {\operatorname{argmax}\limits_{\mathbf{y}^{(t)} \in \mathcal{Y}^{{(t)} \ast}}{{\log p}\hspace{0pt}\left( \mathbf{y}^{(t)} \middle| \mathbf{X} \right)}}},`$

where $`\mathcal{Y}^{{(t)} \ast}`$ denotes all possible translation sequences. Typically beam search approximates Eq. (4), during which the AED generates $`m`$-step output token $`{\hat{y}}_{m}^{(t)}`$ in a left-to-right autoregressive manner:

$`\mathbf{h}`$$`{= {{Encoder}\hspace{0pt}(\mathbf{X})}},`$$`{\hat{y}}_{m}^{(t)}`$$`{= {{Decoder}\hspace{0pt}\left( y_{1:{m - 1}}^{(t)},\mathbf{h} \right)}}.`$

Similarly, an ASR decoder generates $`n`$-step source token $`{\hat{y}}_{n}^{(s)}`$, and ASR targets to feed the multi-tasked loss computation (Eq. (3)):

$`{{\hat{y}}_{n}^{(s)} = {{ASRDecoder}\hspace{0pt}\left( y_{1:{n - 1}}^{(s)},\mathbf{h} \right)}},`$

where both ST and ASR decoders share the same encoder. Note that ST performance has been shown to benefit from encoder initialization with pre-trained ASR parameters (Eq. (2.1))  \[34\].

### 2.2 Shortcomings of the Direct Approach

First and foremost, we are interested in improving the explainability of E2E ST. Considering that the human process for speech-to-text translation entails first recognizing and then translating, the lack of an intermediate ASR output in the direct approach is a major divergence from our natural thought process. Direct E2E ST models with ASR multi-tasking do generate ASR targets during training, but the ASR decoder (Eq. (7)) is parallel to the ST decoder (Eq. (6)) – in other words, ST predictions are not conditioned on any ASR predictions. Therefore, our first desideratum is to simultaneously generate ASR and ST predictions such that each ST word may be explained by a corresponding ASR word. Further our objective is not to simply build explainable ST models at all costs, but rather to enhance high-performance models with explainability – this means that we cannot sacrifice translation quality or latency. This latter consideration is particularly important for streaming models. Therefore our second desideratum is that our explainable models are as good as direct E2E ST toplines in terms of both translation quality and latency.

## 3 Proposed Framework

We first propose to allow E2E ST models to predict word-aligned ASR and ST sequences simultaneously by formulating a general framework for generating target words out-of-order while remembering how to re-order them via post-editing to obtain final translations. We then present two variants of such operation sequences which model absolute positions or relative shifts for target tokens to enable the aforementioned delayed re-ordering ability.

### 3.1 Word-Aligned Simultaneous E2E ASR and ST

Suppose that word-level alignments between two languages are known.¹¹1In this work, we use the statistical tool MGIZA++ \[35\], which performs data-driven alignment without needing additional linguistic resources. Let us first re-formulate the ASR and ST targets as word sequences $`{\overline{\mathbf{y}}}^{(s)} = \left\{ {\overline{\mathbf{y}}}_{i}^{(s)} \right\}_{i = 1}^{{\overline{L}}^{(s)}}`$ and $`{\overline{\mathbf{y}}}^{(t)} = \left\{ {\overline{\mathbf{y}}}_{j}^{(t)} \right\}_{j = 1}^{{\overline{L}}^{(t)}}`$, where $`{\overline{L}}^{(s)}`$ and $`{\overline{L}}^{(t)}`$ denote the number of source and target words. Note that we represent single words, $`{\overline{\mathbf{y}}}_{i}^{(s)}`$ or $`{\overline{\mathbf{y}}}_{j}^{(t)}`$, as a sequence of sub-word tokens. We can then define word-aligned ASR/ST target sequences consisting of tuples of $`\left( {\overline{\mathbf{y}}}_{i}^{(s)},\left\{ {\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)} \right\}_{k = 1}^{{\overline{L}}_{i}^{(t)}} \right)`$, where each source word $`{\overline{\mathbf{y}}}_{i}^{(s)}`$ is aligned to $`{\overline{L}}_{i}^{(t)}`$ target words in a one-to-many mapping. Note that some source or target tokens may actually be un-aligned, so we must augment the respective vocabularies with special tokens \[NO_SRC\] or \[NO_TGT\]. Next, consider that simply concatenating all $`i`$-steps of target word chunks produces an out-of-order translation if there is any re-ordering between source and target languages (e.g., in Figure 1.a), thereby obscuring the intended meaning.

To resolve this out-of-order problem in word-aligned ASR/ST sequences, we propose augmenting each tuple with $`{\overline{L}}_{i}^{(t)}`$ post-editing operations $`\left\{ {\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(o)} \right\}_{k = 1}^{{\overline{L}}_{i}^{(t)}}`$, obtaining an operation tuple:

$`{\mathbf{a}_{i} = \left( {\overline{\mathbf{y}}}_{i}^{(s)},\left\{ {\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)},{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(o)} \right\}_{k = 1}^{{\overline{L}}_{i}^{(t)}} \right)}.`$

These post-editing operations perform rule-based re-ordering of each aligned target word $`{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}`$ by inserting them into correct positions, recovering the original order of the target translation – we discuss particular rule sets to realize this function in the next section.

We can now define an operation sequence $`\mathbf{A} = \left\{ \mathbf{a}_{i} \right\}_{i = 1}^{M}`$ and train an AED to predict $`\mathbf{A}`$ by replacing Eq. (2.1) with:

$`\mathcal{L}^{({O\hspace{0pt}P\hspace{0pt}S})}`$$`= {{\log p}\hspace{0pt}\left( \mathbf{A} \middle| \mathbf{X} \right)}`$$`{= {\sum\limits_{i = 1}^{M^{\star}}{{\log p}\hspace{0pt}\left( y_{i}^{\star} \middle| {y_{1:{i - 1}}^{\star},\mathbf{X}} \right)}}},`$

where $`y_{i}^{\star}`$ denotes elements of the collapsed representation of $`\mathbf{a}_{i}`$ (i.e., words are collapsed into their token sequences) and $`M^{\star}`$ denotes the length of our target sequence. Note that operation sequences are modeled autoregressively by first predicting the source, then the target, and finally the operation (Eq. (3.1)). On the other hand, the direct E2E ST model trained to maximize Eq. (3) does not model any explicit relations between its parallel ASR and ST predictions. Further note that we only replace the function of the main decoder Eq. (6) in our operation sequence AED models, meaning we do not need to sacrifice benefits of ASR multi-tasking (§2.1).

### 3.2 Defining Post-Editing Operations

We examine two ways to represent the insertion position information of post-editing operations: the first uses absolute positions (e.g, Figure 1.b) and the second uses relative shifts (e.g., Figure 1.c).

#### 3.2.1 Absolute Positional Operation Sequence

We define absolute positional sequences with Backus-Naur form as:

- 
  $`<\mathbf{A}>`$ ::= $`<\mathbf{A}>`$$`<\mathbf{a}_{i}^{\ast}>`$\[EOS\] $`|`$ $`<\mathbf{a}_{1}^{\ast}>`$\[EOS\]
- 
  $`<\mathbf{a}_{i}^{\ast}>`$ ::= $`<{\overline{\mathbf{y}}}_{i}^{(s)}>`$\[BL\]$`<\mathbf{T}>`$
- 
  $`<\mathbf{T}>`$ ::= $`<\mathbf{T}>`$$`<{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}>`$$`<{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(o)}>`$ $`|`$ $`<{\overline{\mathbf{y}}}_{i\hspace{0pt}1}^{(t)}>`$$`<{\overline{\mathbf{y}}}_{i\hspace{0pt}1}^{(o)}>`$
- 
  $`<{\overline{\mathbf{y}}}_{i}^{(s)}>`$ ::= $`{\overline{\mathbf{y}}}_{i}^{(s)}`$ $`|`$ \[NO_SRC\]
- 
  $`<{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}>`$ ::= $`{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}`$ $`|`$ \[NO_TGT\]
- 
  $`<{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(o)}>`$ := \[$`n`$\] $`|`$ \[$`- 1`$\],

where $`::=`$ and $`|`$ denote definition and choice, respectively; \[BL\] and \[EOS\] denote the blank symbol for separating source and target words and the end of the sequence, respectively; $`{\overline{\mathbf{y}}}_{i}^{(s)}`$, $`{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}`$ denotes $`i`$-th source word in the transcription and $`k`$-th target word which corresponds to $`{\overline{\mathbf{y}}}_{i}^{(s)}`$; and $`\lbrack n\rbrack`$ (where $`n \in {\mathbb{N}}`$) denotes the position of $`{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}`$ in the translation. We set $`n`$ as $`- 1`$ when $`{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}`$ is \[NO_TGT\].

To restore the transcription and translations, we prepare sufficient-length buffers for the source transcription $`\mathcal{S}`$ and the target translation $`\mathcal{T}`$. Note that all elements of the $`\mathcal{S}`$ and $`\mathcal{T}`$ are initialized with \[NO_SRC\] or \[NO_TGT\]. During run-time, our model outputs the absolute positional sequence tokens, and these tokens are inserted into FIFO queue $`\mathcal{Q}`$. When the model outputs the post-editing command token $`<{\overline{\mathbf{y}}}_{k\hspace{0pt}i}^{(o)}>`$, we obtain sequences of $`<{\overline{\mathbf{y}}}_{i}^{(s)}>`$\[BL\]$`<{\overline{\mathbf{y}}}_{i}^{(t)}>`$$`<{\overline{\mathbf{y}}}_{k\hspace{0pt}i}^{(o)}>`$ or $`<{\overline{\mathbf{y}}}_{i}^{(t)}>`$$`<{\overline{\mathbf{y}}}_{k\hspace{0pt}i}^{(o)}>`$ by dequeuing $`\mathcal{Q}`$. Then the $`i`$-th element of $`\mathcal{S}`$ is rewritten with $`{\overline{\mathbf{y}}}_{i}^{(s)}`$ when the dequeued sequence includes source word. And the $`<{\overline{\mathbf{y}}}_{k\hspace{0pt}i}^{(o)}>`$-th element of $`\mathcal{T}`$ is rewritten with $`<{\overline{\mathbf{y}}}_{k\hspace{0pt}i}^{(t)}>`$. If $`<{\overline{\mathbf{y}}}_{k\hspace{0pt}i}^{(o)}>`$ indicates $`- 1`$, we skip this step. This step is repeated until the end of the sequence, and we obtain the source transcription and the target translations from $`\mathcal{S}`$ and $`\mathcal{T}`$.

#### 3.2.2 Relative Shift Operation Sequences

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2211.05967/assets/x4.png)

Fig. 2: Restoring transcription and translation from the relative shift operation sequence. \[EOP\] operations are omitted for space. \* denotes a marker and positions of the write-heads are highlighted.

\[13\] originally proposed an explainable MT model which models insertions using a moving write header – we adapt this approach for simultaneous ASR/ST. In \[13\], the operation sequence is defined as the stack operations of the read-head $`p_{r}^{(s)}`$ for the transcription $`\mathcal{S}`$ and the write-head $`p_{w}^{(t)}`$ for translation $`\mathcal{T}`$ using following operations:

- 
  POP_SRC moves $`p_{r}^{(s)}`$ right by one token
- 
  SET_MARKER (SM) inserts a marker symbol into $`\mathcal{T}`$ at $`p_{w}^{(t)}`$
- 
  JMP_FWD (JF) moves $`p_{w}^{(t)}`$ to the closest left maker position
- 
  JMP_BWD (JB) moves $`p_{w}^{(t)}`$ to the closest right maker position
- 
  INSERT($`t`$) inserts a target token $`t`$ into $`\mathcal{T}`$ at the position $`p_{w}^{(t)}`$

Since we do not know the transcription in advance, we remove $`p_{r}^{(s)}`$ and POP_SRC. Instead, we define write-head $`p_{w}^{(s)}`$ for transcription $`\mathcal{S}`$. We also revise INSERT into as follows:

- 
  INSERT-t($`t`$) inserts a target token $`t`$ into $`\mathcal{T}`$ at $`p_{w}^{(t)}`$
- 
  INSERT-s($`s`$) inserts a src tok $`s`$ into $`\mathcal{S}`$ at $`p_{w}^{(s)}`$ and moves $`p_{w}^{(s)}`$ right

Our relative shift operation sequences are obtained in the same manner as \[13\] and are defined with Backus-Naur form as:

- 
  $`<\mathbf{A}>`$ ::= $`<\mathbf{A}>`$$`<\mathbf{a}_{i}^{\ast}>`$\[EOS\] $`|`$ $`<\mathbf{a}_{1}^{\ast}>`$\[EOS\]
- 
  $`<\mathbf{a}_{i}^{\ast}>`$ ::= $`<{\overline{\mathbf{y}}}_{i}^{(s)}>`$$`<\mathbf{T}>`$\[EOP\]
- 
  $`<\mathbf{T}>`$ ::= $`<\mathbf{T}>`$$`<{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(o)}>`$$`<{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}>`$ $`|`$ $`<{\overline{\mathbf{y}}}_{11}^{(o)}>`$$`<{\overline{\mathbf{y}}}_{11}^{(t)}>`$
- 
  $`<{\overline{\mathbf{y}}}_{i}^{(s)}>`$ ::= $`{\overline{\mathbf{y}}}_{i}^{(s)}`$ $`|`$ \[NO_SRC\]
- 
  $`<{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}>`$ ::= $`{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}`$ $`|`$ \[NO_TGT\]
- 
  $`<{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(o)}>`$ ::= $`<{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(o)}>`$$`<{\overline{y}}_{i\hspace{0pt}k\hspace{0pt}j}^{(o)}>`$ $`|`$ $`<{\overline{y}}_{i\hspace{0pt}k\hspace{0pt}j}^{(o)}>`$
- 
  $`<{\overline{y}}_{i\hspace{0pt}k\hspace{0pt}j}^{(o)}>`$ ::= \[JF\] $`|`$ \[JB\] $`|`$ \[SM\] $`|`$ \[NO_OPS\],

where \[EOP\] and \[NO_OPS\] denote the end of operation sequence for $`<{\overline{\mathbf{y}}}_{i}^{(s)}>`$ and no operation is required for $`<{\overline{\mathbf{y}}}_{i\hspace{0pt}k}^{(t)}>`$, respectively.

Similar to the absolute positional sequence, the relative shift operation sequence is inserted into the FIFO queue $`\mathcal{Q}`$ during run-time. When the model outputs \[EOP\], we obtain $`<\mathbf{a}_{i}^{\ast}>`$ by dequeuing $`\mathcal{Q}`$. Then, we can update $`\mathcal{S}`$ and $`\mathcal{T}`$ based on the operations. This operation is repeated until the end of the sequence, resulting in transcription and translations from $`\mathcal{S}`$ and $`\mathcal{T}`$ by removing the marker symbols. Figure 2 depicts how the transcriptions and translations are restored from the proposed relative shift operation sequence.

|  |  |  |  | En$`\rightarrow`$De |  |  | En$`\rightarrow`$Fr |  |  |
|----|----|----|----|----|----|----|----|----|----|
| ID | Model | Type | \# Params | WER($`\downarrow`$) | BLEU($`\uparrow`$) | AL($`\downarrow`$) | WER($`\downarrow`$) | BLEU($`\uparrow`$) | AL($`\downarrow`$) |
| A1 | Direct E2E ASR \[37\] (Topline) | Offline | 45M | 7.7 / 6.7 | no ST | n/a | 9.9 / 8.1 | no ST | na |
| A2 | Direct E2E ST \[38\] (Topline) | Offline | 63M | no ASR | 24.8 / 22.9 | n/a | no ASR | 35.9 / 32.9 | n/a |
| A3 | Direct E2E ASR \[37\] (Topline) | Streaming | 45M | 12.0 / 9.9 | no ST | 3365 | 17.9 / 14.2 | no ST | 3279 |
| A4 | Direct E2E ST \[38\] (Topline) | Streaming | 63M | no ASR | 22.2 / 20.1 | 5750 | no ASR | 32.1 / 29.5 | 5648 |
| B1 | Simul. E2E ASR/ST w/ Absolute Position | Offline | 63M | 12.1 / 12.0 | 20.4 / 17.9 | n/a | 14.8 / 16.2 | 28.7 / 26.0 | n/a |
| B2 | Simul. E2E ASR/ST w/ Relative Shift | Offline | 63M | 10.1 / 9.9 | 25.2 / 22.1 | n/a | 12.5 / 11.3 | 35.8 / 32.9 | n/a |
| B3 | Simul. E2E ASR/ST w/ Absolute Position | Streaming | 63M | 18.7 / 17.2 | 18.4 / 16.5 | 5795 | 25.3 / 23.4 | 28.7 / 24.5 | 5684 |
| B4 | Simul. E2E ASR/ST w/ Relative Shift | Streaming | 63M | 16.1 / 14.9 | 23.1 / 20.1 | 5786 | 19.3 / 16.9 | 32.1 / 29.6 | 5770 |

Table 1: Comparison of simultaneous E2E ASR/ST models using absolute position vs. relative shift operation sequences across offline and streaming settings. Better results between these two methods are bolded and any simultaneous results which reach/surpass the topline results of single-task E2E models are further underlined. ASR and ST performances, as measured by $`\%`$ WER and BLEU, are shown on tst-COMMON / tst-HE of MuST-C English$`\rightarrow`$German and English$`\rightarrow`$French \[31\]. Average Lagging (AL) \[36\] is shown for streaming models.

## 4 Results

### 4.1 Experimental setup

Data: We conduct experiments using the English-German and English-French pairs of the MuST-C corpus \[31\]. We apply the tokenizer of the Moses toolkit \[39\] for training sentences. Then we remove the samples whose length ratio between the source and target sequences is greater than five or the target sequence length is longer than 150 tokens. To compute the word alignment, we use MGIZA++ \[35\]. For training MGIZA++ model, we set the number of iterations to 5 for HMM, Model 1, Model2, Model 3, and 10 for Model 4; and we use the deficient distortion model for the empty word to reduce the mapping of the target word into \[NO_SRC\].

Models: Models are trained using ESPnet \[37, 38\]. We use $`4000`$-vocabulary and $`16000`$-vocabulary BPE \[40\] units for source transcription and target operation sequence. We also use $`16000`$-vocabulary BPE units for direct E2E toplines for a fair comparison. ASR and ST models both use conformer encoders \[41, 42\] with $`12`$ blocks, $`4`$ heads, $`31`$ kernel size, $`2048`$ feed-forward dim, and $`256`$ attention dim. We initialize the ST encoder with pre-trained ASR parameters for faster convergence. ASR and ST attentional decoders consist of $`6`$ blocks, $`4`$ heads, and $`2048`$ feed-forward dim. Streaming models follow the blockwise method in \[43\] with $`40`$ block size, $`16`$ hop size, and $`16`$ look-ahead. All models are trained for $`40`$ epochs and decoded using $`10`$ beam and $`2048`$ sim chunk length for streaming with repeat detection \[43\].

Evaluation: We evaluate ASR via word error rate (WER$`\downarrow`$) and ST via de-tokenized case-insensitive BLEU($`\uparrow`$) \[44\]. We evaluate streaming latency via Average Lagging (AL$`\downarrow`$) \[36\]; for separate ASR/ST baselines, we consider the latency of both tasks together.

### 4.2 Results and Discussion

Table 1 presents our main results on two language pairs across offline and streaming settings. We found that the relative shift operation sequence produces better translations than absolute position operation sequences for both offline (B1 vs. B2) and streaming models (B3 vs. B4). The absolute positions appear to inhibit generalization, and we found that these models skewed towards overly short hypotheses even when brevity penalty was applied during beam search. On the other hand, the relative position information from the relative shift operation sequence appears to avoid these pitfalls. These explainable models achieve comparable performance compared to topline E2E ST models which solely optimize toward translation quality in the offline setting (A2 vs. B2). Impressively, in the streaming setting, our operation sequence models even surpass the performance of dedicated toplines without additional latency (A4 vs. B4), suggesting that the ability to delay re-ordering is particularly useful for streaming ST. This improvement is particularly noticeable in the English-German tst-COMMON set, where the relative shift operation sequence yields $`0.9`$ BLEU gain; we found that this particular set required the most re-ordering between source and target languages.

Since our ST models are also simultaneously predicting ASR transcriptions, we examine the ASR quality compared to topline models which solely focus on ASR. In Table 1, it is clear that the burden of producing explainable ST predictions hinders our operation sequence models from matching the topline ASR models – operation sequence models must not only transcribe, but also do so in a way that explains the translations. We also noted that decoding longer translations caused the simultaneous transcriptions to degrade in WER; we do not seek to optimize the ASR side of this trade-off in this work. Nonetheless, the simultaneous transcriptions are reasonable outputs which enable our desired word-level explainability. Further, these transcriptions come with no additional latency, so our simultaneous ASR/ST models are faster than two separate dedicated models performing both tasks.

Finally, we are interested in the re-ordering capability of our operational sequence methods during streaming, where systems need to generate translations while still reading the speech signal. We can apply operation sequences on-the-fly instead of during post-editing. This allows our systems to produce target words according to the order that they are spoken (monotonically) instead of waiting until all of the preceding target words have been produced (as in direct E2E systems). As shown in Figure 3, relative shift operation sequence models consistently generate better partial hypotheses during streaming than topline direct models. For instance in the En-Fr case, this advantage is most apparent for very early hypotheses and the gap between the models closes as hypotheses reach their full lengths.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2211.05967/assets/x5.png)

Fig. 3: Comparison of partially generated ST hypotheses during streaming between relative shift operation sequence models vs. direct ST toplines, measured by absolute BLEU difference on dev sets.

## 5 conclusion

We propose to build E2E models which simultaneously perform ASR and ST by replacing targets with operation sequences which describe word-level alignments and post-editing commands for re-ordering target language words which were produced out-of-order. Our models using relative shift operation sequences achieve explainability without sacrificing translation quality or achievement. Further, these models actually outperformed direct E2E ST toplines in streaming settings due to an improved word re-ordering ability.¹¹footnotetext: Authors acknowledge Graham Neubig’s invaluable contributions.
## References
- \[1\] A. Waibel, “Interactive translation of conversational speech,” Computer, vol. 29, no. 7, pp. 41–48, 1996.
- \[2\] H. Inaguma, B. Yan, S. Dalmia, et al., “ESPnet-ST IWSLT 2021 offline speech translation system,” in Proc. IWSLT 2021, 2021, pp. 100–109.
- \[3\] T. K. Lam, S. Schamoni, and S. Riezler, “Cascaded models with cyclic feedback for direct speech translation,” in Proc. ICASSP 2021, 2021, pp. 7508–7512.
- \[4\] W. Zhang, Z. Ye, H. Tang, et al., “The USTC-NELSLIP offline speech translation systems for IWSLT 2022,” in Proc. IWSLT 2022, 2022, pp. 198–207.
- \[5\] A. Bérard, O. Pietquin, C. Servan, and L. Besacier, “Listen and translate: A proof of concept for end-to-end speech-to-text translation,” arXiv preprint arXiv:1612.01744, 2016.
- \[6\] P. Bahar, T. Bieschke, and H. Ney, “A comparative study on end-to-end speech to text translation,” in Proc. ASRU 2019, 2019, pp. 792–799.
- \[7\] H. Inaguma, Y. Higuchi, K. Duh, et al., “Orthros: Non-autoregressive end-to-end speech translation with dual-decoder,” in Proc. ICASSP 2021, 2021, pp. 7503–7507.
- \[8\] B. Zhang, B. Haddow, and R. Sennrich, “Revisiting end-to-end speech-to-text translation from scratch,” arXiv preprint arXiv:2206.04571, 2022.
- \[9\] M. Sperber, G. Neubig, J. Niehues, and A. Waibel, “Attention-passing models for robust and data-efficient end-to-end speech translation,” Trans. of the ACL, vol. 7, pp. 313–325, 2019.
- \[10\] P. Bahar, T. Bieschke, R. Schlüter, and H. Ney, “Tight integrated end-to-end training for cascaded speech translation,” in Proc. SLT 2021, 2021, pp. 950–957.
- \[11\] S. Dalmia, B. Yan, V. Raunak, et al., “Searchable hidden intermediates for end-to-end models of decomposable sequence tasks,” in Proc. NAACL, 2021, pp. 1882–1896.
- \[12\] B. Yan, P. Fernandes, S. Dalmia, et al., “CMU’s IWSLT 2022 dialect speech translation system,” in Proc. IWSLT 2022, May 2022, pp. 298–307.
- \[13\] F. Stahlberg, D. Saunders, and B. Byrne, “An operation sequence model for explainable neural machine translation,” in Proc. EMNLP, Nov. 2018, pp. 175–186.
- \[14\] Q. Dong, M. Wang, H. Zhou, S. Xu, B. Xu, and L. Li, “Consecutive decoding for speech-to-text translation,” in Proc. AAAI, 2021, vol. 35, pp. 12738–12748.
- \[15\] O. Weller, M. Sperber, C. Gollan, and J. Kluivers, “Streaming models for joint speech recognition and translation,” in Proc. of EACL, Apr. 2021, pp. 2533–2539.
- \[16\] Y. Liu, J. Zhang, H. Xiong, et al., “Synchronous speech recognition and speech-to-text translation with interactive decoding,” in Proc. AAAI, 2020, pp. 8417–8424.
- \[17\] H. Le, J. Pino, C. Wang, et al., “Dual-decoder transformer for joint automatic speech recognition and multilingual speech translation,” in Proc. COLING, 2020, pp. 3520–3533.
- \[18\] B. Yan, S. Dalmia, Y. Higuchi, G. Neubig, F. Metze, A. Black, and S. Watanabe, “Ctc alignments improve autoregressive translation,” arXiv preprint arXiv:2210.05200, 2022.
- \[19\] J. Gu, Q. Liu, and K. Cho, “Insertion-based decoding with automatically inferred generation order,” Trans. of the ACL, vol. 7, pp. 661–676, 2019.
- \[20\] D. Emelianenko, E. Voita, and P. Serdyukov, “Sequence modeling with unconstrained generation order,” Advances in Neural Information Processing Systems, vol. 32, 2019.
- \[21\] A. Góis, K. Cho, and A. Martins, “Learning non-monotonic automatic post-editing of translations from human orderings,” arXiv preprint arXiv:2004.14120, 2020.
- \[22\] Q. Ran, Y. Lin, P. Li, and J. Zhou, “Guiding non-autoregressive neural machine translation decoding with reordering information,” in Proc. AAAI, 2021, vol. 35, pp. 13727–13735.
- \[23\] W. Xu and M. Carpuat, “Editor: an edit-based transformer with repositioning for neural machine translation with soft lexical constraints,” Trans. of ACL, vol. 9, pp. 311–328, 2021.
- \[24\] N.-Q. Pham, T.-S. Nguyen, T.-L. Ha, et al., “The IWSLT 2019 KIT speech translation system,” in Proc. SIGSLT, 2019.
- \[25\] M. Ma, L. Huang, H. Xiong, et al., “STACL: Simultaneous translation with implicit anticipation and controllable latency using prefix-to-prefix framework,” in Proc. ACL, 2019, pp. 3025–3036.
- \[26\] N. Arivazhagan, C. Cherry, I Te, W. Macherey, P. Baljekar, and G. Foster, “Re-translation strategies for long form, simultaneous, spoken language translation,” in Proc. of ICASSP, 2020, pp. 7919–7923.
- \[27\] M. Sperber, H. Setiawan, et al., “Consistent transcription and translation of speech,” Trans. of the ACL, vol. 8, pp. 695–709, 2020.
- \[28\] J. Chen, M Ma, R. Zheng, and L. Huang, “Direct simultaneous speech-to-text translation assisted by synchronized streaming ASR,” in Proc. of ACL-IJCNLP, Aug. 2021, pp. 4618–4624.
- \[29\] A. Anastasopoulos, O. Bojar, J. Bremerman, et al., “Findings of the IWSLT 2021 evaluation campaign,” in Proc. of IWSLT, Aug. 2021, pp. 1–29.
- \[30\] Q. Dong, Y. Zhu, M. Wang, and L. Li, “Learning when to translate for streaming speech,” in Proc. ACL, 2022, pp. 680–694.
- \[31\] M. A. Di Gangi, R. Cattoni, L. Bentivogli, M. Negri, and M. Turchi, “MuST-C: a Multilingual Speech Translation Corpus,” in Proc. NAACL, 2019, pp. 2012–2017.
- \[32\] R. J. Weiss, J. Chorowski, N. Jaitly, et al., “Sequence-to-Sequence Models Can Directly Translate Foreign Speech,” in Proc. Interspeech, 2017, pp. 2625–2629.
- \[33\] S. Karita, N. Chen, T. Hayashi, et al., “A comparative study on transformer vs RNN in speech applications,” in Proc. ASRU, 2019, pp. 449–456.
- \[34\] S. Bansal, H. Kamper, K. Livescu, A. Lopez, and S. Goldwater, “Pre-training on high-resource speech recognition improves low-resource speech-to-text translation,” in Proc. NAACL, 2019, pp. 58–68.
- \[35\] Q. Gao and S. Vogel, “Parallel implementations of word alignment tool,” in Proc. SETQA-NLP, Columbus, Ohio, June 2008, pp. 49–57.
- \[36\] X. Ma, J. Pino, and P. Koehn, “Simulmt to simulst: Adapting simultaneous text translation to end-to-end simultaneous speech translation,” arXiv preprint arXiv:2011.02048, 2020.
- \[37\] S. Watanabe, T. Hori, S. Karita, et al., “Espnet: End-to-end speech processing toolkit,” Proc. Interspeech, 2018.
- \[38\] H. Inaguma, S. Kiyono, K. Duh, et al., “ESPnet-ST: All-in-one speech translation toolkit,” in Proc. ACL, 2020, pp. 302–311.
- \[39\] P. Koehn, H. Hoang, A. Birch, et al., “Moses: Open source toolkit for statistical machine translation,” in Proc. ACL, 2007, pp. 177–180.
- \[40\] R. Sennrich, B. Haddow, and A. Birch, “Neural machine translation of rare words with subword units,” Proc. ACL, 2015.
- \[41\] A. Gulati, J. Qin, C.-C. Chiu, et al., “Conformer: Convolution-augmented Transformer for Speech Recognition,” in Proc. Interspeech 2020, 2020, pp. 5036–5040.
- \[42\] P. Guo, F. Boyer, X. Chang, et al., “Recent developments on espnet toolkit boosted by conformer,” in Proc. ICASSP, 2021.
- \[43\] E. Tsunoo, Y. Kashiwagi, and S. Watanabe, “Streaming transformer asr with blockwise synchronous beam search,” in Proc. SLT, 2021, pp. 22–29.
- \[44\] M. Post, “A call for clarity in reporting BLEU scores,” in Proc. WMT, 2018, pp. 186–191.
