---
arxiv_id: '2206.08738'
title: Detecting Adversarial Examples in Batches -- a geometrical approach
authors:
- Danush Kumar Venkatesh
- Peter Steinbach
submitted: '2022-06-17'
categories:
- cs.LG
arxiv_url: https://arxiv.org/abs/2206.08738
github_repo: ''
source: arxiv-html
converter: pandoc
llm_remediated: false
citations_resolved: 0/0
citations_resolved_at: '2026-07-07T19:26:47+00:00'
references_parsed: 0
arxiv_version: ''
---

# Detecting Adversarial Examples in Batches - a geometrical approach

Danush Kumar Venkatesh    Peter Steinbach

###### Abstract

Many deep learning methods have successfully solved complex tasks in computer vision and speech recognition applications. Nonetheless, the robustness of these models has been found to be vulnerable to perturbed inputs or adversarial examples, which are imperceptible to the human eye, but lead the model to erroneous output decisions. In this study, we adapt and introduce two geometric metrics, *density* and *coverage*, and evaluate their use in detecting adversarial samples in batches of unseen data. We empirically study the metrics using MNIST and two real-world biomedical datasets from MedMNIST, subjected to two different adversarial attacks. Our experiments show promising results for both metrics to detect adversarial examples. We believe that his work can lay the ground for further study on these metrics’ use in deployed machine learning systems to monitor for possible attacks by adversarial examples or related pathologies such as dataset shift.

Machine Learning, ICML

\

## 1 Introduction

Neural networks (NN) are machine learning algorithms that have been demonstrated to achieve human-level performance in computer vision (e.g. deep neural network architectures, DNNs), in speech recognition (Hinton et al., 2012) and many other tasks of academic, industrial or cultural value. However, studies have shown that the stability (or robustness) of such models can be distorted by adding small human imperceptible perturbations to the input samples (Szegedy et al., 2014; Goodfellow et al., 2014) so that predictions by these systems are misguided. Such imperceptibly perturbed samples are termed as *adversarial samples* (AEs) and can be crafted by *adversarial attacks* (Brendel et al., 2018). This unenviable property of the NNs causes major security concerns with regard to deployment in real-world applications like medical imaging (Lundervold & Lundervold, 2019) or self-driving cars (Evtimov et al., 2017) to name a few.

#### Our Contributions

In this paper, we propose the use of *density* and *coverage* (Naeem et al., 2020) as metrics for detecting AEs in batches of images. We have adopted these two metrics from the field of generative modeling and quality assessment of generative adversarial networks (GANs).

In detail, we contribute the following aspects to the literature:

- •
  We adopted *density* and *coverage* as metrics and propose a *model-agnostic* method to detect adversarial samples in batches of images.
- •
  We empirically demonstrate the capacity of the metric using standard and real-world datasets subjected to both white- and black-box attacks aligned to recommendations in (Carlini & Wagner, 2017).
- •
  We study the root cause of *density* and *coverage* curves obtained from the aforementioned experiments.
- •
  We have improved upon the computational efficiency of the proposed metrics.

Our work aspires to remain computational efficient and useful for practitioners. In other words, our approach only requires practical applications to ”train” a k-nearest neighbor tree on the used training set of images or affiliated NN embeddings obtained from domain-specific application, e.g. NN derived image classification. Due to the availability of performant open-source libraries such as faiss (Johnson et al., 2017) we consider this to be a minimal burden for practical concerns.

## 2 Related Work

In this section, we provide a short summary of AE detection techniques and provide some context to image quality metrics from a generative modelling perspective in order to provide context for *density* and *coverage*.

#### Detecting Adversarial Examples

With a statistical approach, (Grosse et al., 2017) proposed a model agnostic and kernel-based two-sample test. The mean discrepancy distance metric (Gretton et al., 2012) was used to detect AEs from clean samples. Detectors based on kernel density and Bayesian uncertainty estimates were presented to distinguish AEs from noisy and clean samples (Feinman et al., 2017). A manifold learning strategy based on expansion models was used to study the local dimensionality of adversarial regions. Local Intrinsic Dimensionality (LID) estimates were used further to distinguish AEs (Ma et al., 2018). Based on the assumption that the feature space of models can be fitted to class conditional Gaussian distributions by Gaussian discriminant analysis, a confidence score was suggested to detect out-of-distribution and AEs based on Mahalanobis distance (Lee et al., 2018). A k-nearest neighbor(k-NN) model was fit to the feature space of DNNs utilizing influence function score (Cohen et al., 2020). Similarly, neighbor context encoder (NCE), a transformer-based detector, was proposed by (Mao et al., 2020). (Li & Li, 2017) built a cascade classifier to distinguish AEs using the PCA of samples from each output layer of DNNs. For a more detailed review of the field, we refer to (Aldahdooh et al., 2022).

#### GAN image generation quality metrics

The qualitative performance of a GAN model is commonly assessed with respect to two main aspects: fidelity (quality) and diversity (variability) of the generated (fake) images with respect to the training data set. Multiple metrics have been presented to evaluate the models based on these aspects. More recently, two separate metrics, precision and recall, were formulated based on the uniform density assumptions of real and fake data distributions (Sajjadi et al., 2018). Further (Kynkäänniemi et al., 2019) proposed improved precision (IP) and recall (IR), wherein the probability density functions were used to cautiously construct the manifolds using the *k*th nearest neighbor. Certain drawbacks of IP&IR such as outlier susceptibility or the computational expense were overcome by *density* and *coverage*, that was implemented as prdc (Naeem et al., 2020).\

## 3 Adversarial example detection

In this work, we focus on image classification and attacking such a trained model to produce AEs in an untargeted fashion, i.e., the model predicts any other class than the ground truth. In this section, we introduce *density* and *coverage* mathematically.

Let $`\mathcal{P}`$(X) be the distribution of the dataset images and $`\mathcal{Y}`$ be the corresponding class labels. Following the underlying assumptions from prdc, we sample two different distributions, the model dataset $`\mathcal{P}\hspace{0pt}{(X_{m})}`$ and a hold-out set, the validation dataset $`\mathcal{P}\hspace{0pt}{(X_{v})}`$. Let the labeled set, $`S_{m} = {(x_{i},y_{i})}_{i = 1}^{m}`$ be sampled from $`\mathcal{P}\hspace{0pt}{(X_{m})}`$ and $`S_{v} = {(x_{j},y_{j})}_{j = 1}^{n}`$ from $`\mathcal{P}\hspace{0pt}{(X_{v})}`$, with *m* and *n* the number of samples in model and validation dataset respectively. Let $`f`$ be a model trained on $`\mathcal{P}\hspace{0pt}{(X_{m})}`$. The model $`f`$ is then attacked using $`S_{v}`$ to generate the adversarial sample $`x_{a}`$ with the distribution $`\mathcal{P}\hspace{0pt}{(X_{a})}`$.

### 3.1 Density

In the context of generative modeling, density quantifies the quality and coverage assesses the variability of the generated (fake) images. These metrics, have been developed with improved precision and recall(IP&IR) as their backbone. Let $`\mathcal{R}`$ and $`\mathcal{Q}`$ be reference and query distributions, then IP&IR is computed by constructing the manifolds $`\Phi_{r}`$ and $`\Phi_{q}`$, one for each distribution, with the hypersphere radii estimated by the *k*-nearest neighbor. The binary decision of whether $`q_{i}`$ sampled from query distribution $`\mathcal{Q}`$ lies in the manifold $`\Phi_{r}`$ constitutes precision, whereas recall measures the portion of $`\mathcal{R}`$ falling inside $`\Phi_{q}`$. *Density* is computed by normalizing the constructed manifold and counting the number of reference manifold spheres containing $`q_{i}`$. It is defined as

``` math
\text{density} = {\frac{1}{k\hspace{0pt}S}\hspace{0pt}{\sum\limits_{j = 1}^{S}{\sum\limits_{i = 1}^{T}1_{q_{j} \in {\mathcal{M}\hspace{0pt}{(r_{1},\cdots,r_{S})}}}}}}
```

where *S* and *T* are the number of reference and query samples, $`1_{(.)}`$ the binary decision function and $`\mathcal{M}`$ the defined reference manifold.

### 3.2 Coverage

*Coverage* computation focuses on constructing manifolds only for the reference samples in contrast to IR and measures the fraction of these manifolds containing at least one query sample. *Coverage* is defined as

``` math
\text{coverage} = {\frac{1}{T}\hspace{0pt}{\sum\limits_{i = 1}^{T}1_{{\exists{j\hspace{0pt}\text{~s.t.~}\hspace{0pt}q_{j}}} \in {\mathcal{M}\hspace{0pt}{(r_{1},\cdots,r_{S})}}}}}
```

*Density* remains unbounded, that is, it can be larger than $`1`$, whereas *coverage* is bounded between $`0`$ and $`1`$. The manifolds are defined by

``` math
{\mathcal{M}\hspace{0pt}\left( r_{1},\cdots,r_{S} \right)} = {\bigcup\limits_{i = 1}^{S}{\mathcal{H}\hspace{0pt}\left( r_{i},{{nn}_{k}\left( r_{i} \right)} \right)}}
```

where $`\mathcal{H}\hspace{0pt}{(r_{i},\overset{\sim}{r})}`$ is the sphere around $`r_{i}`$, with the radius $`\overset{\sim}{r}`$ estimated by $`{nn}_{k}\left( r_{i} \right)`$ that denotes the *k* nearest neighbor to $`r_{i}`$, expressed in $`\mathbf{R}^{d}`$. The computation of these manifolds exposes a severe runtime overhead in the original implementation of prdc. We overcome this problem using the tree structure of faiss (Johnson et al., 2017) to find the nearest neighbor to a given data point and efficiently construct the hyperspheres. Appendix D is referred for related analysis.

## 4 Experiments

We briefly explain the datasets and the adversarial attacks which were explored to quantify the proposed metric.

### 4.1 Datasets

The study was performed on the standard MNIST dataset (LeCun et al., 1998) and also on 2D real-world biomedical datasets from MedMNIST (Yang et al., 2021). The datasets are described as follows,\

MNIST: The dataset consists of $`70000`$ grayscale images with $`10`$ classes corresponding to handwritten digits $`0`$ to $`9`$. The dataset was split into a model dataset size of $`67900`$ images and a validation dataset of $`2100`$ images.\

PathMNIST: This dataset consists of $`107,180`$ pathological tissue images consisting of $`9`$ classes describing different pathological tissue types. A total of $`5359`$ images were used to form the validation dataset, and the remaining was the model dataset. This dataset is encoded as a set of RGB images (Kather et al., 2019).\

OrganMNIST: $`58,850`$ abdominal CT scan grayscale images corresponding to $`11`$ different classes are contained in this dataset (Bilic et al., 2019). A validation set size of $`4708`$ images was chosen for our study.\

All the images were of $`28`$x$`28`$ pixels. Each dataset was split using a stratification strategy. The proportional values are available in Appendix B. For illustration, we show a subset of each dataset in  Figure 1.

|     |
|-----|
|     |
|     |
|     |

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/dataset/mnistImages.png)

(a) MNIST

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/dataset/pathImages.png)

(b) PathMNIST

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/dataset/organImages.png)

(c) OrganMNIST

Figure 1: The images of the different dataset. Each image represents a unique class in the dataset.

### 4.2 Procedure for metric computation

A necessary preliminary step for the computation of the metric and the interpretation of our results is the dataset curation. We conducted this as follows:

1.  1.
    Dataset split: The dataset of interest is split into $`2`$ different sets, a set to train the model, and a hold-out dataset. We mention these splits as the model dataset and the validation dataset. The model dataset is used to train the CNN model, and the validation dataset is used exclusively to perform the attack and generate adversarial samples.
2.  2.
    Generate adversarial samples: Attack the trained model using the validation dataset and generate the *validation adversarial samples*

However, if adversarial samples are already present, step 2 can be bypassed.

The procedure to differentiate and detect the adversarial samples using the proposed metrics is as follows:

1.  1.
    Reference *density*/*coverage*: compute the metrics between the *model dataset* and the *validation dataset* which serves as reference value before the model is attacked.
2.  2.
    Adversarial *density*/*coverage*: compute the metrics between the *model dataset* and the *validation adversarial samples*.

The reference manifold is always constructed on the model dataset($`\Phi_{m}`$) and the query samples constitute either the validation dataset or the generated AEs to compute the reference and adversarial metric respectively.

In our setup, *density* and *coverage* are metrics to describe the geometric distribution of samples from the holdout set (e.g. a query batch) with respect to the distribution of the data used for training. Along this line of thought, our method can be applied on any input data that represents a query batch uniquely in an appropriate n-dimensional space. In other words, these metrics can be computed on the entire benign or malignant query image batch. Alternatively, both metrics can also be calculated from the embedded space of logits of a trained classifier or autoencoder as obtained from the query image. In the following, we will report the results obtained for the entire images only. Interested readers curious about the outcomes in the embedded space are kindly deferred to Appendix C.

### 4.3 Adversarial attacks

We focus our study on two attacks namely, Fast gradient sign method (FGSM) (Goodfellow et al., 2014) and the Boundary attack (Brendel et al., 2018). We use these two extremes to probe the rich space of possible attacks already published. As our method is independent of the attack, we consider it highly likely that our reported results map to other attacks too.

#### FGSM

A type of white-box attack in which AEs are crafted by computing the gradient of the loss function for the given input image such that the loss increases along the steepest direction. In our study, it was meaningful to vary the magnitude of perturbation, $`\epsilon`$, in the range $`\lbrack 0,1\rbrack`$ in steps of $`0.05`$ as the accuracy of the models decreased to below-par values.

#### Boundary attack

In this algorithm, AEs are generated by reducing the distance between the original (non-adversarial) sample and adversarial target (i.e., a sample from a target class to be injected) by tracing along the decision boundary and making a step-update in combination with a suitable perturbation from a given distribution.

Foolbox (Rauber et al., 2017) was used to attack our trained models and to generate the AEs.

### 4.4 CNN Model

We designed two different model architectures for the classifier model on which the attack was performed: A $`5`$ layer model for the MNIST dataset. For both the MedMNIST datasets, an architecture of $`5`$ convolutional layers followed by $`3`$ fully-connected layers was created. Both MedMNIST models are only different in the number of channels for the input layer. The model details are provided in Appendix B.

To define the uncertainties of the metrics for the attacks, uncertainty estimates were obtained for both reference and adversarially altered metrics. For this, the respective image datasets were split into smaller batches of $`100`$ images each. Then, any metric of interest was collected for each of these sub-batches. From the ensemble of measurements obtained this way, we compute the $`0.25`$, $`0.5`$, and $`0.75`$ quantiles of any metrics of interest for this study.

For the FGSM attack, we use $`20`$ values for epsilon, and both metrics are computed for each of these values. In the case of the Boundary attack, we also examine the metrics by varying the batch size of the images depending on the datasets. Throughout this study, $`k`$ as of Equation 1 and  Equation 2 was maintained at a value of $`5`$ in any nearest neighbor query performed. The code and the implementation of experiments are provided in (anonymized authors, ).

## 5 Results & Discussion

We summarize and discuss the results of the experiments in this section. The CNN model trained for the MNIST dataset achieved an accuracy of $`99.2\%`$. Similarly, the models for the medical images were trained to achieve an accuracy of $`96\%`$ and $`99.1\%`$ for PathMNIST and OrganMNIST, respectively. The model accuracy for biomedical images was on par with the benchmark results from (Yang et al., 2021). The AEs generated for different datasets are shown in  Figure 2 to provide an overview and intuition.

### 5.1 FGSM attack

The variation of *density* and *coverage* for the FGSM attack on the datasets is shown in  Figure 3(a).

|  |  |  |  |  |  |
|----|----|----|----|----|----|
|  Dataset |  Original | FGSM |  |  |  Bdy. |
|  MNIST |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_mnst_20_epi0.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_mnst_20_epi1.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_mnst_20_epi2.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_mnst_20_epi3.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/bdy_attack/bdy_mnst_20.png) |
|  |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_mnst_13_epi0.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_mnst_13_epi1.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_mnst_13_epi2.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_mnst_13_epi3.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/bdy_attack/bdy_mnst_13.png) |
|  PathMNIST |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_path_2011_epi0.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_path_2011_epi1.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_path_2011_epi2.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_path_2011_epi2.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/bdy_attack/bdy_path_2011.png) |
|  |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_path_4911_epi0.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_path_4911_epi1.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_path_4911_epi2.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_path_4911_epi3.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/bdy_attack/bdy_path_4911.png) |
|  OrganMNIST |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_org_20_epi0.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_org_20_epi1.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_org_20_epi2.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_org_20_epi3.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/bdy_attack/bdy_org_20.png) |
|  |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_org_711_epi0.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_org_711_epi1.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_org_711_epi2.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/adv_images/fgsm_org_711_epi3.png) |  ![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/bdy_attack/bdy_org_711.png) |
|  $`\epsilon_{\text{FGSM}}`$ |  |  $`0.3`$ |  $`0.75`$ |  $`1.0`$ |  |

Figure 2: Example images from the original datasets as well as generated by two adversarial attacks from our classification models. The $`2^{n\hspace{0pt}d}`$ column indicates the original (benign) samples. $`3^{r\hspace{0pt}d}`$, $`4^{t\hspace{0pt}h}`$ and $`5^{t\hspace{0pt}h}`$ columns correspond to FGSM attack with $`\epsilon_{\text{FGSM}}`$ being set to $`0.3,0.75`$ and $`1`$ as indicated. The last column of images are AEs generated from the boundary attack.

#### MNIST

The reference *density* for the MNIST dataset achieves a value of $`0.997`$ (horizontal line in Figure 3(a) $`1^{s\hspace{0pt}t}`$ row). After the attack, the adversarial *density* varies from the reference *density* for the complete range of epsilon values. It increases gradually with an increase in epsilon and crosses over towards small values of *density* at $`\epsilon = 0.38`$, after which it gradually decreases further with the increase in epsilon. The reference *coverage* for the dataset has a value of $`0.39`$. Similar to adversarial *density*, the adversarial *coverage* for the MNIST dataset increases initially with a peak value of $`0.46`$, then crosses over the reference value at $`\epsilon = 0.28`$ and decreases further towards smaller values.

#### MedMNIST

The *density* variation for the OrganMNIST dataset shows similar behavior to the MNIST dataset. The reference *density* has a value of $`1.01`$, and the adversarial *density* peaks at $`1.17`$ and reduces to $`0.22`$ for the maximum epsilon value. However, for the case of PathMNIST, a non-identical trend in the adversarial *density* was noticed. This dataset has a reference *density* value of $`0.83`$, and the adversarial *density* raises thereafter with the increase in epsilon, having the maximum value of $`4.56`$. The behavior and variation in *coverage* for all three datasets appears similar to each other. In the case of OrganMNIST, the reference *coverage* had a value of $`0.34`$, and the adversarial *coverage* varies between $`0.36`$ and $`0.003`$. The adversarial *coverage* has a sharp peak at $`\epsilon = 0.18`$ for PathMNIST dataset, whereas the reference *coverage* remained at $`0.49`$.

#### Metric performance

As described for each dataset, the reference *density* lies close to the value of $`1`$. This is an expected outcome, as it confirms the i.i.d assumption. It also indicates that the model dataset manifold $`\Phi_{m}`$ is closely packed without outliers along with the validation samples lying in the vicinity (Naeem et al., 2020). From the  Figure 3(a), it is evident that for the full range of epsilon values, both adversarial *density* and *coverage* exhibit a noticeable difference from the reference metric. For the MNIST and OrganMNIST datasets, the adversarial *density* increases in the region between $`\epsilon = 0`$ and $`\epsilon = 0.38`$, which is purely due to geometric reasons. Concerning the definition of the FGSM attack, the gradient tensor with respect to the input image is nudged in a direction with the scaling value of epsilon to create an adversarial sample. As the computation of *density* and *coverage* is based on the binary function, as seen in  Equation 1 and  Equation 3, with the initial increase of epsilon, the generated AEs move into the multitude of model dataset manifolds $`\Phi_{m}`$, and this augments the number of hyperspheres $`\mathcal{H}`$ containing AEs thereby contributing to the initial increase of the metric.

With higher epsilon values, the decreasing trend of the *density* curves underlines that the manifold, $`\Phi_{m}`$ and AEs become separated. The continuous increase of adversarial *density* in the case of PathMNIST revealed that the generated AEs are classified into a particular class with the increase in epsilon, thereby confining the AEs into a small region of hyperspheres and contributing to the *density* increase. As the value obtained for *coverage* depends on the number of samples considered for its computation, a variation in reference values *coverage* between the datasets is expected. From the *coverage* plots, the decreasing *coverage* values with the increase in epsilon indicate the reduction in the diversity of samples generated. That is, the generated AEs always tend to be classified into a particular class with the increase of epsilon. A visualization in a space of reduced dimensionality using pymde (Agrawal et al., 2021) and an analysis of it are provided in  Appendix C.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/fgsm/fgsm_combined_bands.png)

(a) FGSM attack

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/bdy_attack/bdy_attack_combined.png)

(b) Boundary attack

Figure 3: The variation of *density* and *coverage* for the reference validation dataset as well as for AEs obtained from it. The red line corresponds to metrics obtained from the benign (reference) validation set. The blue line refers metrics obtained from malignant AEs derived from the same validation set. The green and gray bands indicate uncertainty intervals between $`{25\%} - {50\%}`$ and $`{50\%} - {75\%}`$ quantiles estimated from ensemble measurements respectively. Each column corresponds to the specified dataset as indicated. Rows report results for *density* or *coverage* respectively. *Density* and *coverage* from AEs are distinctly different from reference values.

### 5.2 Boundary attack

The behaviour of *density* and *coverage* between the benign and AEs generated using the boundary attack for the datasets are shown in  Figure 3(b).

#### MNIST

For this dataset, the reference *density* lies between $`0.9`$ and $`1.1`$ for various batch sizes. The adversarial *density* values were comparatively low, within the range of $`0.11`$ and $`0.22`$. The reference *coverage* achieves a value of $`0.14`$ compared to the adversarial *coverage* value of $`0.004`$ for the entire batch of our validation dataset. An interesting result, as noticed in the $`2^{n\hspace{0pt}d}`$ column of  Figure 3(b), is that *coverage* increases gradually with an increase in image batch sizes and finally reaches that of reference or adversarial values.

#### MedMNIST

In the case of PathMNIST, an averse behavior of *density* to that of MNIST was noticed. The reference *density* has a value of $`0.831`$ and the adversarial *density* with the value of $`10.12`$ for the whole batch of validation dataset. A slight difference in values was measured in the OrganMNIST dataset,:with reference *density* lying between $`0.96`$ and $`1.05`$ compared to adversarial *density* values of $`2.2`$ and $`3.1`$. The *coverage* values for these datasets showed the same inclination as seen in the MNIST dataset: the gradual increase of *coverage* with the increase in batch sizes. For the entire batch of images, we obtained the reference *coverage* for PathMNIST at a value of $`0.49`$ and the adversarial *coverage* value of $`0.65`$. For the OrganMNIST, the reference *coverage* was at $`0.34`$ in meager contrast to adversarial *coverage* of $`0.24`$.

#### Metric performance

Similar to the FGSM attack, the reference values of density and coverage remained the same. However, for the case of MNIST and PathMNIST datasets, a discernible difference was noticed between the reference and adversarial metrics. The progressive increase of *coverage* with an increase in the batch size reassures us of the dependency on the number of samples involved in its computation. The results also exhibit the distinctive capability of *density* and *coverage* to detect the adversarial samples. The variation in these metrics reveals that the pathological characteristics of AEs were more adverse than those crafted by the FGSM attack.

### 5.3 Admixture of adversarial samples

In a real-world scenario, practitioners aspire to detect AEs in batches of samples to classify on a case by case basis at best. In order to learn, how precise *density* and *coverage* can be used for this purpose we intermixed AEs into subsets of benign (untouched) samples in proportionate quantities. We then computed our metrics of interest. The metrics variation for each of the dataset for the FGSM attack is shown in  Figure 4. The results of the similar analysis for the boundary attack is available in the supplementary material.

As seen from these results, *density* and *coverage* show a distinctively progressive deviation from the full batch of benign samples (i.e. a deviation away from the reference metric), to that of the batch of AEs for the attack. These experiments further advocate that *density* and *coverage* can serve as a measure to detect AEs.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/mixture/fgsm_mix_combined_changed.png)

Figure 4: The proportionate mixture of adversarial samples and benign samples for the FGSM attack. *Density* and *coverage* for the entire batch of benign(reference) and adversarial samples are indicated by black and purple lines, respectively. The red, blue, and green lines indicate the variation of the metrics for $`25\%`$, $`50\%`$, and $`75\%`$ proportional mixture of AEs with benign samples. *Density* variation is specified in the first row, followed by *coverage* in the second row. Above values of 25% of malignant sample admixture, *density* and *coverage* values start to diverge above dataset-specific thresholds.

### 5.4 Discussion

An essential property of our proposed metrics is that they are model-agnostic. In that spirit, they can directly operate on the feature space or the data itself. This overlooks the development of auxillary models for AE detection. In contrast to the usefulness of *density* and *coverage* for detection of AEs, we see a few shortcomings from our analysis above: for one, the sensitivity of *coverage* to the number of samples; further the number of nearest neighbors used for computation, which is inherent to the mathematical formulation of *coverage* computation is another.

## 6 Conclusion

We have adopted two metrics for quality of sample generation from generative modeling, i.e. *density* and *coverage*. We improved their design to make them of practical use in realistic runtime scenarios. We then studied how *density* and *coverage* behave in the face of AEs for classifying images from MNIST or from benchmark datasets in the medical domain, PathMNIST and OrganMNIST. Our experiments confirm that both *density* and *coverage* are susceptible to effects maliciously introduced in AEs which are imperceptible to humans. Moreover, we demonstrated that admixtures of AEs into batches of untouched benign images by $`25\%`$ or more distort the values of *density* or *coverage* in such a fashion, that it makes both metrics viable candidates to flag such batches of images as malignant in practice. We believe that this can and should be subject of further study. With a potential addition of statistical hypothesis tests to differentiate provided (unseen) samples from a known training set using *density* and *coverage*, our approach can be of substantial support in fraud detection and monitoring of adversarial attacks in deployed machine learning systems. Apart from this, applications in detecting dataset shift or similar pathologies specific to a presented dataset in comparison to the training set should also be considered in future work.
## References
- Agrawal et al. (2021) Agrawal, A., Ali, A., and Boyd, S. Minimum-distortion embedding. *arXiv*, 2021.
- Aldahdooh et al. (2022) Aldahdooh, A., Hamidouche, W., Fezza, S. A., and Déforges, O. Adversarial example detection for dnn models: A review and experimental comparison. *Artificial Intelligence Review*, pp.  1–60, 2022.
- (3) anonymized authors, V. n2gem. URL [https://anonymous.4open.science/r/n2gem/README.md](https://anonymous.4open.science/r/n2gem/README.md). anonymized on Mar 21, 2022.
- Beyer et al. (2020) Beyer, L., Hénaff, O. J., Kolesnikov, A., Zhai, X., and Oord, A. v. d. Are we done with imagenet?, 2020. URL [https://arxiv.org/abs/2006.07159](https://arxiv.org/abs/2006.07159).
- Biggio & Roli (2018) Biggio, B. and Roli, F. Wild patterns: Ten years after the rise of adversarial machine learning. *Pattern Recognition*, 84:317–331, 2018. ISSN 0031-3203. doi: https://doi.org/10.1016/j.patcog.2018.07.023.
- Bilic et al. (2019) Bilic, P., Christ, P. F., Vorontsov, E., Chlebus, G., Chen, H., Dou, Q., Fu, C.-W., Han, X., Heng, P.-A., Hesser, J., et al. The liver tumor segmentation benchmark (lits). *arXiv preprint arXiv:1901.04056*, 2019.
- Brendel et al. (2018) Brendel, W., Rauber, J., and Bethge, M. Decision-based adversarial attacks: Reliable attacks against black-box machine learning models. In *International Conference on Learning Representations*, 2018.
- Carlini & Wagner (2017) Carlini, N. and Wagner, D. Adversarial examples are not easily detected: Bypassing ten detection methods. In *Proceedings of the 10th ACM workshop on artificial intelligence and security*, pp.  3–14, 2017.
- Carter et al. (2021) Carter, B., Jain, S., Mueller, J. W., and Gifford, D. Overinterpretation reveals image classification model pathologies. *Advances in Neural Information Processing Systems*, 34, 2021.
- Cohen et al. (2020) Cohen, G., Sapiro, G., and Giryes, R. Detecting adversarial samples using influence functions and nearest neighbors. In *Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition*, pp.  14453–14462, 2020.
- Evtimov et al. (2017) Evtimov, I., Eykholt, K., Fernandes, E., Kohno, T., Li, B., Prakash, A., Rahmati, A., and Song, D. Robust physical-world attacks on machine learning models. *arXiv preprint arXiv:1707.08945*, 2(3):4, 2017.
- Feinman et al. (2017) Feinman, R., Curtin, R. R., Shintre, S., and Gardner, A. B. Detecting adversarial samples from artifacts. *arXiv preprint arXiv:1703.00410*, 2017.
- Finlayson et al. (2019) Finlayson, S. G., Bowers, J. D., Ito, J., Zittrain, J. L., Beam, A. L., and Kohane, I. S. Adversarial attacks on medical machine learning. *Science*, 363(6433):1287–1289, 2019.
- Goodfellow et al. (2014) Goodfellow, I. J., Shlens, J., and Szegedy, C. Explaining and harnessing adversarial examples. *arXiv preprint arXiv:1412.6572*, 2014.
- Gretton et al. (2012) Gretton, A., Borgwardt, K. M., Rasch, M. J., Schölkopf, B., and Smola, A. A kernel two-sample test. *The Journal of Machine Learning Research*, 13(1):723–773, 2012.
- Grosse et al. (2017) Grosse, K., Manoharan, P., Papernot, N., Backes, M., and McDaniel, P. On the (statistical) detection of adversarial examples. *arXiv preprint arXiv:1702.06280*, 2017.
- Hinton et al. (2012) Hinton, G., Deng, L., Yu, D., Dahl, G. E., Mohamed, A.-r., Jaitly, N., Senior, A., Vanhoucke, V., Nguyen, P., Sainath, T. N., et al. Deep neural networks for acoustic modeling in speech recognition: The shared views of four research groups. *IEEE Signal processing magazine*, 29(6):82–97, 2012.
- Howard & Gugger (2020) Howard, J. and Gugger, S. *Deep Learning for Coders with fastai and PyTorch*. O’Reilly Media, 2020.
- Johnson et al. (2017) Johnson, J., Douze, M., and Jégou, H. Billion-scale similarity search with gpus. *arXiv preprint arXiv:1702.08734*, 2017.
- Kather et al. (2019) Kather, J. N., Krisam, J., Charoentong, P., Luedde, T., Herpel, E., Weis, C.-A., Gaiser, T., Marx, A., Valous, N. A., Ferber, D., et al. Predicting survival from colorectal cancer histology slides using deep learning: A retrospective multicenter study. *PLoS medicine*, 16(1):e1002730, 2019.
- Kynkäänniemi et al. (2019) Kynkäänniemi, T., Karras, T., Laine, S., Lehtinen, J., and Aila, T. Improved precision and recall metric for assessing generative models. *Advances in Neural Information Processing Systems*, 32:3927–3936, 2019.
- LeCun et al. (1998) LeCun, Y., Bottou, L., Bengio, Y., and Haffner, P. Gradient-based learning applied to document recognition. *Proceedings of the IEEE*, 86(11):2278–2324, 1998.
- Lee et al. (2018) Lee, K., Lee, K., Lee, H., and Shin, J. A simple unified framework for detecting out-of-distribution samples and adversarial attacks. *Advances in neural information processing systems*, 31, 2018.
- Li & Li (2017) Li, X. and Li, F. Adversarial examples detection in deep networks with convolutional filter statistics. In *Proceedings of the IEEE International Conference on Computer Vision*, pp.  5764–5772, 2017.
- Lundervold & Lundervold (2019) Lundervold, A. S. and Lundervold, A. An overview of deep learning in medical imaging focusing on mri. *Zeitschrift für Medizinische Physik*, 29(2):102–127, 2019.
- Ma et al. (2018) Ma, X., Li, B., Wang, Y., Erfani, S. M., Wijewickrema, S., Schoenebeck, G., Song, D., Houle, M. E., and Bailey, J. Characterizing adversarial subspaces using local intrinsic dimensionality. *arXiv preprint arXiv:1801.02613*, 2018.
- Mao et al. (2020) Mao, X., Chen, Y., Li, Y., He, Y., and Xue, H. Learning to characterize adversarial subspaces. In *ICASSP 2020-2020 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP)*, pp.  2438–2442. IEEE, 2020.
- Naeem et al. (2020) Naeem, M. F., Oh, S. J., Uh, Y., Choi, Y., and Yoo, J. Reliable fidelity and diversity metrics for generative models. In *International Conference on Machine Learning*, pp. 7176–7185. PMLR, 2020.
- Rauber et al. (2017) Rauber, J., Brendel, W., and Bethge, M. Foolbox: A python toolbox to benchmark the robustness of machine learning models. In *Reliable Machine Learning in the Wild Workshop, 34th International Conference on Machine Learning*, 2017. URL [http://arxiv.org/abs/1707.04131](http://arxiv.org/abs/1707.04131).
- Sajjadi et al. (2018) Sajjadi, M. S., Bachem, O., Lucic, M., Bousquet, O., and Gelly, S. Assessing generative models via precision and recall. In *Proceedings of the 32nd International Conference on Neural Information Processing Systems*, pp.  5234–5243, 2018.
- Szegedy et al. (2014) Szegedy, C., Zaremba, W., Sutskever, I., Bruna, J., Erhan, D., Goodfellow, I., and Fergus, R. Intriguing properties of neural networks. In *2nd International Conference on Learning Representations, ICLR 2014*, 2014.
- Yang et al. (2021) Yang, J., Shi, R., Wei, D., Liu, Z., Zhao, L., Ke, B., Pfister, H., and Ni, B. Medmnist v2: A large-scale lightweight benchmark for 2d and 3d biomedical image classification. *arXiv preprint arXiv:2110.14795*, 2021.

## Appendix A Adversarial attacks

We followed the categorization of adversarial attacks alongside (Biggio & Roli, 2018) into:

- •
  White-box-attacks: The attacker has complete information regarding the target model, including the model parameters and the architecture. AEs are crafted by formulating an optimization problem.
- •
  Gray-box-attacks: In this category, the attacker has information regarding the model architecture or the training data used to devise the parameters but no direct access to the learned parameters.
- •
  Black-box-attacks: Adversarial examples are crafted by a query to the trained model and observing the predicted label.

#### FGSM

The adversarial sample for the attack is formally defined as,

``` math
x_{a} = {x + {{\epsilon \ast s}\hspace{0pt}i\hspace{0pt}g\hspace{0pt}n\hspace{0pt}{({{\nabla_{x}J}\hspace{0pt}{(\theta,x,y)}})}}}
```

where $`\epsilon`$ is the magnitude of perturbation, $`\theta`$ are the network parameters, and $`J\hspace{0pt}{(\theta,x,y)}`$ is the cost or loss function of the trained model.\
\

#### Boundary attack

This is a rejection sampling algorithm to construct AEs. An adversarial target (i.e. a sample from a target class to be injected) is used as the starting point in this attack. A random perturbation drawn from the Gaussian distribution is added to the benign source image iteratively. Then, a step-update along the decision boundary between the original (non-adversarial) sample and the AE is performed such that the distance between them is minimized. Since this attack has information only about the predicted class label, multiple queries to the model have to be made before convergence, i.e. before a misclassification based on the AE is performed by the victim model. The AEs are generated by the minimization $`{\parallel{x - x_{a}}\parallel}_{2}^{2}`$, such that the new adversarial is defined by $`x_{a} = {x_{a}^{s - 1} + \eta_{s}}`$, where $`\eta_{s}`$ is the random perturbation from a suitable distribution for a particular step $`s`$.

## Appendix B Model information

The model architectures for the MNIST and the MedMNIST dataset are shown in  Table 1 and  Table 2 respectively. Throughout the model, the kernel size was maintained at with radius $`3`$ along with a drop-out rate of $`0.5`$. To have a stabilizing effect on learning and to speed up the learning process, *batch normalization* was introduced in the convolution layers. Except for the last layer, *ReLU* was applied as the activation function and the *softmax activation* for the last layer. The Adam optimizer and negative log-likelihood loss function was used. To estimate optimizer hyper parameters a $`1`$ cycle policy was employed, wherein the learning rate is varied for each mini-batch to train the model. The optimal learning rate for the dataset was found and the model was fine-tuned to achieve the best possible outcome. All models were trained using FastAI (Howard & Gugger, 2020). The split of samples into the model dataset and validation set for the datasets are indicated in  Table 3.

| Layer No. |  Layer information                  | Description |
|-----------|-------------------------------------|-------------|
| 1         |  Conv2D + ReLU + MaxPool            | 16 filters  |
| 2         |  Conv2D + ReLU + MaxPool + Dropout  | 32 filters  |
| 3         |  Flatten                            | 800 neurons |
| 3         |  Dense + ReLU + Dropout             | 512 neurons |
| 4         |  Dense + ReLU                       | 64 neurons  |
| 5         |  Dense + Softmax                    | 10 classes  |

Table 1: The MNIST classifier architecture

| Layer No. |  Layer information                                   | Description |
|-----------|------------------------------------------------------|-------------|
| 1         |  Conv2D + Batchnorm + ReLU                           | 32 filters  |
| 2         |  Conv2D + Batchnorm + ReLU + MaxPool                 | 32 filters  |
| 3         |  Conv2D + Batchnorm + ReLU                           | 64 filters  |
| 4         |  Conv2D + Batchnorm + ReLU                           | 64 filters  |
| 4         |  Conv2D(padding=$`1`$) + Batchnorm + ReLU + MaxPool  | 64 filters  |
| 5         |  Dense + ReLU                                        | 128 neurons |
| 6         |  Dense + ReLU                                        | 64 neurons  |
| 7         |  Dense + Softmax                                     | \*classes   |

Table 2: The MedMNIST classifier architecture. \*classes$`= 9`$ and $`11`$ for PathMNIST and OrganMNIST respectively

| Dataset |  Total no. of samples |  No. of samples in model dataset |  No. of samples in validation set |
|----|----|----|----|
| MNIST |  $`70000`$ |  $`67000`$ |  $`2100`$ |
| PathMNIST |  $`107180`$ |  $`101821`$ |  $`5359`$ |
| OrgMNIST |  $`58850`$ |  $`54142`$ |  $`4708`$ |

Table 3: The number of samples considered in each of the datasets

## Appendix C Metrics analysis

The value of density and coverage depends on the output of the binary function—a short explanation of how density increases or decreases are shown in Figure 5.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/den_expl.png)

Figure 5: The graphical interpretation of the metric. The blue circles indicate the manifold of the benign samples and the red points correspond to AEs. The manifolds are formed with $`k = 2`$.

The plot from left to right can be associated with the metrics variation for initial values of epsilon. As epsilon increases for the FGSM attack, the AEs (*red points*) move into the benign sample hyperspheres (*blue spheres*) and augment the number of hyperspheres for the AE. This contributes to the increase of the metric. However, with higher epsilon values, the AEs have moved away from the benign sample manifolds, thereby decreasing the metrics. For the PathMNIST dataset, an adverse behavior of adversarial density is noticed. As epsilon increases, the AEs initially move into the benign hyperspheres and get accommodated thereafter; the AEs for this dataset lies close to the benign samples. This is reflected in the continuous increase of density.\

The difference in the model’s predictions to the true label for the range of epsilon values for each dataset is shown in the histogram plots, i.e. Figure 6, Figure 7 and Figure 8. For the MNIST dataset, see  Figure 6, beyond values of $`\epsilon = 0.55`$, class $`8`$ is predicted as the dominant class, which is also evident from  Figure 9(a) (increase in the number of *blue points*). We also analyzed the relative change in volume of each cluster of the predicted class, see  Figure 10(a). This indicated that, the volume of the cluster pertaining to class $`8`$ gradually increases with an increase in epsilon (the yellow color indicates large difference in values). This can be mapped to cluster $`8`$ attracting more and more malignant samples and eventually becoming the dominant predicted class label. For the PathMNIST dataset, the class $`2`$ is the predominantly predicted class as seen from  Figure 7 for high values of $`\epsilon`$. As noticed from  Figure 10(b) the volume of each class cluster decreases and also remain nearly the same (the black regions for $`\epsilon = {0.75,1}`$) with an increase in epsilon as compared to class $`2`$. The  Figure 9(b) further indicates that for higher values of epsilon, the classes cluster into a confined space contributing to the increase of density well above $`1`$. On the other hand, the coverage decreases as the diversity in the generated AEs decreases. A similar trend to the MNIST dataset was also noticed in the case of the OrganMNIST dataset.

We also analyzed the change in distance between each cluster center between the benign samples and the generated AEs. The pair-wise distance between the benign and AEs were computed, which then served as input for Multi-dimensional scaling to obtain the reduced dimensional coordinates for the cluster centres. As seen from  Figure 11(a) and Figure 11(b), the distance matrix varies for each epsilon value indicating an evident change in cluster centers for MNIST and PathmMNIST datasets. However, the  Figure 11(c) shows that such onset in difference appears only after $`\epsilon = 0.5`$ for the OrganMNIST dataset. These results also purport the variation of the metrics explained in  Figure 5.

The results of an analysis using the embeddings from the last layer of a classification model in contrast to using the input images are shown in  Figure 12. There exists a perceptible difference between the reference and adversarial *density* and *coverage* for each of the datasets. We perceive this as a clear indication, that the morphology of the embedded space is severely different to that of the input space of raw images. However, we see that AEs can be differentiated from benign samples in this scenarios too albeit under different morphological constraints.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/histogram/fgsm_mnist_histogram.png)

Figure 6: The variation between the true and predicted adversarial labels of the MNIST dataset subjected to FGSM attack for different value of epsilons.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/histogram/fgsm_pathmnist_histogram.png)

Figure 7: The variation for the PathMNIST dataset between the true and predicted adversarial labels for different values of epsilons. This illustrates how the attack changes the predicted class labels.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/histogram/fgsm_orgmnist_histogram.png)

Figure 8: Variation between the true and predicted adversarial labels of the OrganMNIST dataset subjected to FGSM attack for different values of epsilons. This illustrates how the attack changes the predicted class labels.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/mnist_pymde.png)

(a) MNIST

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/pathmnist_pymde.png)

(b) PathMNIST

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/organmnist_pymde.png)

(c) OrganMNIST

Figure 9: Reduced dimensionality representation of the adversarial samples for each of the dataset. The colorbar indicates the label of each class in the dataset.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/mnistdiff_vol.png)

(a) MNIST

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/pathmnist_vol.png)

(b) PathMNIST

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/orgmnist_vol.png)

(c) OrganMNIST

Figure 10: The difference in volume between the class clusters for each epsilon. The plots are symmetric, with yellow indicating a distinct difference between cluster volume and black no difference at all.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/mnist_clustercenter.png)

(a) MNIST

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/pathmnist_clustercenter.png)

(b) PathMNIST

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/organmnist_clustercenter.png)

(c) OrganMNIST

Figure 11: The difference in cluster centres between the classes for each epsilon. The y-axis indicates the class labels for benign samples and the y-axis for AEs. The variation of the distance matrices between epsilons indicate a change in cluster distance.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/fgsm_FX_combined.png)

Figure 12: The variation of *density* and *coverage* for the reference validation dataset as well as for AEs using the embeddings. The red line corresponds to metrics obtained from the benign (reference) validation set. The blue line refers metrics obtained from malignant AEs derived from the same validation set. The green and gray bands indicate uncertainty intervals between $`{25\%} - {50\%}`$ and $`{50\%} - {75\%}`$ quantiles estimated from ensemble measurements respectively. Each column corresponds to the specified dataset as indicated. Rows report results for *density* or *coverage* respectively. *Density* and *coverage* from AEs are distinctly different from reference values.

Similar to the FGSM attack, we performed the experiment of mixing AEs into the full batch of validation(benign) samples for the boundary attack. The variation in *density* and *coverage* values is shown in Table 4 and Table 5. These results re-iterate the characteristics of these metrics to distinguish batches of benign samples from adversarial samples.

|              |           |           |          |          |
|--------------|-----------|-----------|----------|----------|
| Dataset      | Density   |           |          |          |
| AE admixture | $`0\%`$   | $`25\%`$  | $`50\%`$ | $`75\%`$ |
| MNIST        | $`1.007`$ | $`0.80`$  | $`0.59`$ | $`0.39`$ |
| PathMNIST    | $`0.83`$  | $`3.002`$ | $`4.92`$ | $`7.91`$ |
| OrganMNIST   | $`1.01`$  | $`1.41`$  | $`1.86`$ | $`2.29`$ |

Table 4: *Density* from batches of benign samples with proportionate admixtures of AEs for the boundary attack.

|              |          |          |          |          |
|--------------|----------|----------|----------|----------|
| Dataset      | Coverage |          |          |          |
| AE admixture | $`0\%`$  | $`25\%`$ | $`50\%`$ | $`75\%`$ |
| MNIST        | $`0.14`$ | $`0.11`$ | $`0.07`$ | $`0.04`$ |
| PathMNIST    | $`0.49`$ | $`0.51`$ | $`0.56`$ | $`0.59`$ |
| OrganMNIST   | $`0.34`$ | $`0.30`$ | $`0.27`$ | $`0.25`$ |

Table 5: *Coverage* from batches of benign samples with proportionate admixtures of AEs for the boundary attack.

## Appendix D Runtime analysis

The analysis of runtime between prdc and our implementations is shown in  Figure 13. We compute the speed up, $`\beta = \frac{T_{p\hspace{0pt}r\hspace{0pt}d\hspace{0pt}c}}{T_{n\hspace{0pt}2\hspace{0pt}g\hspace{0pt}e\hspace{0pt}m}}`$, obtained in using our implementation in comparison to prdc. We have decreased the computation time by nearly $`100\hspace{0pt}x`$ when compared to prdc implementation.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2206.08738/assets/images/mixture/time_analysis_changed.png)

Figure 13: The speed up in runtime between *prdc* and our implementation. Indicates the time for computing both *density* and *coverage* on a dataset with specified sample size and $`512`$ dimensions.

## Review and Author response

In this section, we provide our response to the questions and concerns brought forward by two reviewers during peer-review at the [AdvML](https://advml-frontier.github.io/) workshop with [ICML 2022](https://icml.cc/Conferences/2022/CallForPapers) where this preprint was submitted to. Let us first of all thank the reviewers for investing their time in looking through our article in detail.

### Reviewer $`1`$

1.  1.
    ”It seems that the FGSM attack and MNIST dataset adopted in this paper could not reflect the effect of this method, maybe presenting the performance on Cifar-10 under PGD attack will be more convincing.”\
    Our reply:

    - •
      We have shown the results of the metrics *density* and *coverage* on the MNIST dataset. To improve the complexity of the attack and the dataset used, we have conducted experiments with $`2`$ additional and very different bio-medical datasets, OrganMNIST and PathMNIST. To complement, we also used a black-box attack and hoped that in light of the compute time expense that these two attacks provide enough spectrum on the kind of attack.
    - •
      The results indicated the capability of the metrics *density* and *coverage* to detect adversarial samples in all settings tested. The results in  Section 5.2 discuss these observations.
    - •
      In particular, Figure 3(a) addresses the concern by the reviewer. The text in  Section 5.2 discusses why we believe this to be evidence in favor of our method.
2.  2.
    ”In 3.2, the Density metric is unbounded, and the Coverage is bounded between 0 and 1. why are these two metrics different.”\
    Our reply:

    - •
      As cited in  Section 3.1, the density metric measures the number of benign-sample manifolds containing an adversarial sample. Inherited in the formulation of density, the metric’s value can surpass $`1`$, indicating that the benign-sample neighborhoods and the adversarial samples are very closely packed. Coverage, on the other hand, computes the fraction of benign-sample hyperspheres containing at least one adversarial sample. Both metrics have very different meanings, and in generative modeling, density demonstrates the quality and coverage of the variability of the image for a given dataset.
3.  3.
    ”Through the experiments, under FGSM, in Figure 3 (a) and Figure4, why does PathMNIST express different trends following the changing of epsilon?”\
    Our reply:

    - •
      The causes for the variation of *density* and *coverage* have been discussed in  Section 5.1. Furthermore, dimensionality reduction techniques also describe the reason for such particular trends. The related analysis and results are shown in the  Appendix C.
4.  4.
    ”Figure 3 should be modified to more clear, the legend, x/y-axis, and the line in the figure are not clear.”\
    Our reply:

    - •
      This is a valid statement. We could include an explanation of $`\epsilon`$ which is given in the main text, but not in the caption.
    - •
      We could add in the plot legends the word ”quantiles” (e.g. $`{25\%} - {{50\%}\hspace{0pt}\text{quantiles}}`$) to repeat the caption in the plot. We agree that this may lead to a more quick visual digestion.

### Reviewer $`2`$

1.  1.
    ”The chosen dataset is too simple and should be tested on at least the CIFAR-10 dataset”\
    Our reply:

    - •
      We focussed our introductory study on the MNIST dataset, which we acknowledge as a valuable benchmark dataset to validate a few claims. However, we have experimented with and validated our proposed metrics on $`2`$ very different bio-medical datasets, which we believe are on par with the CIFAR-10 dataset.
    - •
      In addition, we had hoped (but not documented in the text due to space constraints) to explicitly not include too many established benchmark datasets, as various authors Carter et al. (2021); Beyer et al. (2020) have shown problems of overfitting on these in publications.
    - •
      Last, the workshop website did not mention that using CIFAR-10 as a training dataset is a requirement for a submission.
2.  2.
    ”Why are medical datasets selected? In other words, I would like to know in what scenarios medical images are attacked and need to be identified as adversarial examples”\
    Our reply:

    - •
      DNNs have been increasingly used in medical diagnostics and as a tool for physicians in intricate fields of medicine such as radiology and ophthalmology. Trillions of dollars worth of insurance claims are processed by companies leveraging such automated systems. Not only focussing on financial incentives but the vulnerability of algorithms should also be considered in such a high stake field (Finlayson et al., 2019). We considered this situation a ground model, concentrated on medical images, and diversified our validation approach.
3.  3.
    ”Limited novelty. For the problem this paper is trying to solve, the most critical is metric, which has been proposed by Naeem et al.”\
    Our reply:

    - •
      The [AdvML2022 website](https://advml-frontier.github.io/) clearly invites articles on ”Adversarial ML metrics and their interconnections”
    - •
      To our knowledge, *density* and *coverage* are metrics conceived for characterizing the performance of GANs (Naeem et al., 2020).
    - •
      As stated in  Section 1, the novelty of our approach lies in the mapping of metrics from the field of generative modeling to adversarial attacks.
    - •
      Moreover, we have also worked to improve the runtime of the metrics in comparison to the previous implementation, see Appendix D.
4.  4.
    ”The validity of the proposed method needs to be verified with the latest proposed attack”\
    Our reply:

    - •
      In our study, we have experimented with both the extremes of adversarial attacks, i.e., white- and black-box attacks. We believe that the proposed metrics would scale well in detecting adversarial samples as our approach remains model agnostic and only relies on the datasets in hand and the generated adversarial samples.
    - •
      Similar to our reply above, this appears a requirement to us which is not stated on the [AdvML2022 website](https://advml-frontier.github.io/).
